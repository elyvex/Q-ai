---
phase: 01-foundations
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - xtask/src/arch.rs
  - xtask/allowlist.toml
  - docs/04-tasks/active/TASK-001-foundation-gap-closure.md
  - docs/04-tasks/active/README.md
  - docs/04-tasks/completed/TASK-001-foundation-gap-closure.md
  - docs/04-tasks/completed/README.md
  - docs/06-progress/status.md
  - .planning/phases/01-foundations/01-VALIDATION.md
  - scripts/verify-phase1-records.sh
  - docs/06-progress/task-done-rollup.md
  - CHANGELOG.md
files_deleted:
  - docs/04-tasks/active/TASK-001-foundation-gap-closure.md
autonomous: true
requirements:
  - REQ-product-vision
  - REQ-product-principles
  - REQ-goals-non-goals
  - REQ-engineering-baseline
  - REQ-storage-architecture
  - REQ-cli-api
  - REQ-architecture-principles-quality
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Locked decisions D-01 through D-16 govern every final architecture, evidence, lifecycle, and status assertion in this plan."
    - "The architecture gate evaluates both workspace/path and registry/git dependency sources against explicit, fail-closed per-crate policy; a forbidden external edge fails deterministically (D-02, D-03)."
    - "All currently allowed direct workspace-member dependency edges remain green without adding a package, broker, or storage replacement (D-01, D-04)."
    - "The final five roadmap success criteria each have a named plan/task, a repeatable command, and recorded evidence rather than prose-only completion (D-01, D-03)."
    - "The cross-phase `TASK-001-foundation-gap-closure` record moves from active to completed only after tests, lint, architecture, migration, and full-workspace gates are recorded; the active index has no TASK-001 entry, the completed index has exactly one, and the rollup/status/changelog remain consistent (D-02, D-03)."
    - "No phase validator depends on the not-yet-created 01-05 summary; finalization reads Plan 05 itself, the preceding summaries, validation records, and actual gate output, then creates its summary (D-01, D-03)."
    - "Explicit migration readiness, redaction, audited mutations, durable jobs, and the `qai serve` host remain visible in the final evidence matrix (D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16)."
    - "No deferred Quran/search/server/remote-deployment idea, owner decision, or unrelated worktree change is absorbed into Phase 1 closure (D-02, D-04)."
    - "The five final records agree only after a checked-in verifier checks each record independently for its own required IDs, statuses, and lifecycle counts (D-03)."
  artifacts:
    - path: "xtask/src/arch.rs"
      provides: "Registry/git dependency classification, fail-closed external-edge violations, and inline synthetic mutation coverage"
    - path: "xtask/allowlist.toml"
      provides: "Explicit per-crate external registry and git policy"
    - path: "docs/04-tasks/completed/TASK-001-foundation-gap-closure.md"
      provides: "Closed cross-phase task record with plan links and evidence"
    - path: "docs/06-progress/task-done-rollup.md"
      provides: "Newest-first Phase 1 completion entry"
    - path: ".planning/phases/01-foundations/01-VALIDATION.md"
      provides: "Completed Nyquist/task/verification matrix for plans 01-01 through 01-05"
    - path: "CHANGELOG.md"
      provides: "Concise Unreleased record of the foundation gap closure"
    - path: "scripts/verify-phase1-records.sh"
      provides: "Record-specific Phase 1 closure assertions with no cross-record token substitution or runtime product dependency"
  key_links:
    - from: "xtask/src/arch.rs"
      to: "xtask/allowlist.toml"
      via: "pure violations() reads the checked-in workspace and external policy; inline tests exercise registry/git classification"
      pattern: "source|registry_allow|git_allow|violations|Metadata|Dependency"
    - from: "docs/04-tasks/completed/TASK-001-foundation-gap-closure.md"
      to: ".planning/phases/01-foundations/01-VALIDATION.md"
      via: "the closed task record links each plan and its repeatable evidence"
      pattern: "01-0[1-5]|D-0[1-9]|D-1[0-6]"
    - from: ".planning/phases/01-foundations/01-VALIDATION.md"
      to: "docs/06-progress/task-done-rollup.md"
      via: "the phase matrix and newest completion entry report the same gate outcomes"
      pattern: "arch-check|migrate-check|cargo test --workspace"
    - from: "scripts/verify-phase1-records.sh"
      to: "docs/04-tasks/completed/TASK-001-foundation-gap-closure.md"
      via: "the script asserts exact completed-task status and required plan/decision/requirement/criterion/probe/threat tokens in that record"
      pattern: "TASK-001|Status|01-01|D-01|REQ-|C1|PROBE-01|T-01-CFG"
  assumptions:
    - probe_id: "PROBE-05"
      status: "retained-assumption"
      disposition: "The final scope check keeps the executable surface Quran-first and prevents later Quran/search/server behavior from being absorbed into the foundation closure."
      coverage_note: "The canonical PROBE-05 disposition is in 01-01; this plan's source audit and final scope review provide the phase-boundary evidence."
    - probe_id: "PROBE-06"
      status: "retained-assumption"
      disposition: "No foundation model, remote provider, sectarian authority claim, new corpus capability, or package install is introduced."
      coverage_note: "The canonical PROBE-06 disposition is in 01-01; the dependency and scope gates are run before closure."
    - probe_id: "PROBE-07"
      status: "retained-assumption"
      disposition: "The complete engineering matrix is bounded to typed diagnostics, real tests, cancellation, formatting, clippy, documentation, and recovery evidence for touched seams."
      coverage_note: "The canonical PROBE-07 disposition is in 01-01; the final fmt/clippy/test/xtask gates record the enforced subset."
    - probe_id: "PROBE-08"
      status: "retained-assumption"
      disposition: "SQLite remains the local authority and explicit migration remains the only schema mutation path; PostgreSQL portability is not claimed by this phase."
      coverage_note: "The canonical PROBE-08 disposition is in 01-01; `migrate-check` and the readiness/atomicity suites provide the SQLite evidence."
    - probe_id: "PROBE-10"
      status: "retained-assumption"
      disposition: "Correctness and enforced workspace/external dependency boundaries are required; broader UX/performance requirements are not reimplemented here."
      coverage_note: "The canonical PROBE-10 disposition is in 01-01; inline registry/git mutation tests and `arch-check` provide the evidence."
  prohibitions:
    - statement: "MUST NOT mark the task or phase complete without repeatable evidence for all five roadmap criteria."
      status: "unresolved"
      verification: null
      reason: "Spec-less prohibition fallback: retain as flagged-unverified; D-01 and D-03 require evidence-backed closure."
    - statement: "MUST NOT treat a registry or git dependency as allowed merely because its package name is absent from the path-only rule."
      status: "unresolved"
      verification: null
      reason: "Spec-less prohibition fallback: retain as flagged-unverified; the external policy must be explicit and fail closed."
    - statement: "MUST NOT install, upgrade, or silently bless a new package during architecture enforcement."
      status: "unresolved"
      verification: null
      reason: "Spec-less prohibition fallback: retain as flagged-unverified; the phase package audit found no new install requirement."
    - statement: "MUST NOT rewrite unrelated phase-2 work or convert a deferred item into a Phase 1 deliverable."
      status: "unresolved"
      verification: null
      reason: "Spec-less prohibition fallback: retain as flagged-unverified; D-02 and D-04 govern the phase boundary and worktree preservation."

---

<!-- planner-discipline-allow: TASK-001-foundation-gap-closure -->

<objective>
Close FND-07 and the Phase 1 evidence gate without rebuilding any foundation subsystem. This plan implements and verifies D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, and D-16 by extending the existing `xtask` gate to external dependency sources, proving its mutation behavior in tests, and closing the repository task/validation/rollup records only after all repeatable gates pass. It does not install packages, add a broker, change migrations, or absorb later-phase capabilities.

Purpose: Make architecture violations fail closed at the actual dependency boundary and make the brownfield phase auditable from the repository’s own task and progress records.

Output: Explicit external dependency policy, inline synthetic registry/git regression coverage, a completed TASK-001 record with consistent active/completed indexes, a fully populated Phase 1 validation matrix, a record-specific closure checker, and a final status/rollup/changelog entry tied to the five roadmap criteria.
</objective>

<execution_context>
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/workflows/execute-plan.md
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-PATTERNS.md
@.planning/phases/01-foundations/01-VALIDATION.md
@.planning/phases/01-foundations/01-01-SUMMARY.md
@.planning/phases/01-foundations/01-02-SUMMARY.md
@.planning/phases/01-foundations/01-03-SUMMARY.md
@.planning/phases/01-foundations/01-04-SUMMARY.md
@docs/02-architecture/decisions/ADR-0012-workspace-boundaries.md
@docs/02-architecture/decisions/ADR-0002-migration-strategy.md
@docs/02-architecture/decisions/ADR-0003-durable-job-system.md
@docs/02-architecture/decisions/ADR-0009-audit-integrity.md
@xtask/src/arch.rs
@xtask/allowlist.toml
@xtask/src/ci.rs
@.github/workflows/ci.yml
@docs/04-tasks/active/TASK-001-foundation-gap-closure.md
@docs/04-tasks/completed/README.md
@docs/06-progress/task-done-rollup.md
@docs/06-progress/status.md
@CHANGELOG.md
</context>

## Reuse ledger

| Current seam | Demonstrated gap | Smallest closing change |
|---|---|---|
| `xtask::arch::violations` | Filters only `source.is_none()` path edges | Classify registry/git sources and compare them with explicit external policy |
| `xtask/allowlist.toml` | `external.allow` is reserved but unused | Add a per-crate registry/git allow representation populated from current metadata |
| Existing synthetic `arch` tests | Only a forbidden path edge and registry-only clean case exist | Add allowed/forbidden registry and git mutation fixtures |
| Cross-file shell token loops | A token in one record can satisfy a requirement for another record | Add one checked-in POSIX checker with a per-record `require_tokens` helper and exact lifecycle counts |
| CI `arch-check` step | Gate exists but cannot see a forbidden external edge | Keep the existing command and make its test prove the mutation fails |
| Cross-phase `TASK-001-foundation-gap-closure` record and indexes | No final cross-phase closure record or phase evidence matrix | Move the task, remove its active-index entry, add one completed-index entry, append dated evidence, update validation and changelog after gates |

## Context-budget rationale

This plan retains twelve plan-level files only because task 01-05-02 owns one repository lifecycle transition and task 01-05-03 owns independent final-record verification. The `xtask` policy and inline tests remain one two-file subsystem; the task move must touch the task file plus both indexes in one operation to prevent split-brain state; and the final records plus checker are a single validation boundary. Splitting the move from either index would leave the required property false, while splitting the checker from the records it verifies would detach the evidence from its gate. The calibrated projection is 38,000 tokens for three bounded tasks, below the smart-zone threshold, and no runtime dependency, package install, or migration is added.

## Source audit

| SOURCE | ID | Feature or contract | Plan/task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | G-01 | Workspace builds and stable `qai --help` | 01-05-03 | COVERED | Final build/help/catalog evidence is recorded |
| GOAL | G-02 | CLI > env > file > defaults with actionable validation | 01-05-03 | COVERED | Config and foundation tests are linked in the final matrix |
| GOAL | G-03 | Provenance and append-only audit for state changes | 01-05-03 | COVERED | Integrity and fault-injection evidence is linked |
| GOAL | G-04 | Durable jobs enqueue, lease, checkpoint, and cancel without a broker | 01-05-03 | COVERED | Jobs, recovery, lifecycle-audit, and host evidence is linked |
| GOAL | G-05 | `xtask arch-check` and CI reject forbidden dependencies | 01-05-01, 01-05-03 | COVERED | External mutation test and real gate are required |
| REQ | REQ-product-vision | Trustworthy Quran-first boundary | 01-05-03 | COVERED | Scope review rejects later capability absorption |
| REQ | REQ-product-principles | Traceability, redaction, locality, fail-closed behavior | 01-05-03 | COVERED | Evidence matrix links each safety predicate to a command |
| REQ | REQ-goals-non-goals | No foundation model, remote provider, or sectarian authority | 01-05-01, 01-05-03 | COVERED | No package or feature is added |
| REQ | REQ-engineering-baseline | Typed errors, tests, observability, dependency principles, DX | 01-05-01, 01-05-03 | COVERED | Full fmt/clippy/test/CI gates are recorded |
| REQ | REQ-storage-architecture | SQLite authority, explicit migrations, durable outbox | 01-05-03 | COVERED | `migrate-check` and storage evidence are final gates |
| REQ | REQ-cli-api | Executable CLI, stable groups, JSON, remedies, cancellable jobs | 01-05-03 | COVERED | Foundation/catalog/job/host evidence is linked |
| REQ | REQ-architecture-principles-quality | Correctness, maintainability, extensibility, enforced boundaries | 01-05-01, 01-05-03 | COVERED | Explicit external policy plus CI mutation coverage |
| RESEARCH | FND-01 | Stable foundation CLI contract | 01-05-03 | COVERED | Existing foundation test is part of final evidence |
| RESEARCH | FND-02 | Read-only doctor and persisted audit authority | 01-05-03 | COVERED | Foundation/audit tests are linked |
| RESEARCH | FND-03 | Explicit migration readiness | 01-05-03 | COVERED | `migrate-check` and CLI remedy evidence are linked |
| RESEARCH | FND-04 | Audited durable mutations | 01-05-03 | COVERED | Failure-injection and integrity tests are linked |
| RESEARCH | FND-05 | Durable job lifecycle | 01-05-03 | COVERED | Recovery, lifecycle-audit, and host tests are linked |
| RESEARCH | FND-06 | Long-lived worker host and enqueue-only one-shot path | 01-05-03 | COVERED | Host and real CLI tests are linked |
| RESEARCH | FND-07 | Registry/git architecture enforcement | 01-05-01 | COVERED | Pure policy and mutation tests are added |
| RESEARCH | ARCH-05 | Workspace and external dependency policy | 01-05-01 | COVERED | `arch-check` remains the machine gate |
| RESEARCH | CONSTRAINT-01 | No new external package or broker | 01-05-01, 01-05-03 | COVERED | No package legitimacy checkpoint is needed because no install is proposed |
| RESEARCH | CONSTRAINT-02 | Append-only checksummed migrations | 01-05-03 | COVERED | No migration is created; the gate remains green |
| CONTEXT | D-01 | Evidence-driven brownfield gap closure | 01-05 objective, 01-05-03 | COVERED | Final matrix maps each criterion to evidence |
| CONTEXT | D-02 | Roadmap and ADRs govern completion | 01-05 objective, 01-05-02 | COVERED | Closure records cite the phase contract and ADRs |
| CONTEXT | D-03 | Repeatable automated/CLI evidence | 01-05-01, 01-05-03 | COVERED | No criterion closes on prose alone |
| CONTEXT | D-04 | Reuse later code without expanding it | 01-05-01, 01-05-03 | COVERED | External policy and scope review preserve boundaries |
| CONTEXT | D-05 | Explicit migrations | 01-05-03 | COVERED | Migration status/checksum gates are final evidence |
| CONTEXT | D-06 | Read-only doctor | 01-05-03 | COVERED | Fresh-directory acceptance is linked |
| CONTEXT | D-07 | Redacted config/origins | 01-05-03 | COVERED | Config and sentinel tests are linked |
| CONTEXT | D-08 | Stable foundation command groups | 01-05-03 | COVERED | Help/catalog contract is linked |
| CONTEXT | D-09 | Durable domain mutations audited | 01-05-03 | COVERED | Audited mutation tests are linked |
| CONTEXT | D-10 | All-or-nothing state/provenance/audit/outbox | 01-05-03 | COVERED | Fault-injection evidence is linked |
| CONTEXT | D-11 | Job lifecycle audit events | 01-05-03 | COVERED | Lifecycle audit tests are linked |
| CONTEXT | D-12 | Persisted audit verification and recovery guidance | 01-05-03 | COVERED | `qai audit verify`/doctor evidence is linked |
| CONTEXT | D-13 | Long-lived hosts own workers | 01-05-03 | COVERED | Host and enqueue-only evidence is linked |
| CONTEXT | D-14 | Durable named checkpoints | 01-05-03 | COVERED | Recovery evidence is linked |
| CONTEXT | D-15 | Bounded per-kind retry and explicit retry | 01-05-03 | COVERED | Job lifecycle evidence is linked |
| CONTEXT | D-16 | Cooperative cancellation outcomes | 01-05-03 | COVERED | Cancellation/recovery evidence is linked |

All source rows are covered. Deferred ideas, owner-gated dataset/licensing work, remote deployment, and later Quran/search/server capabilities remain excluded. The live-worktree baseline in 01-01 governs every shared source seam; any unrelated Phase 2 delta discovered during execution is explicitly out of scope and must not be staged or edited.

<tasks>

<task type="auto" tdd="true">
  <name>TASK-001 / 01-05-01: Enforce registry and git dependency policy in `xtask`</name>
  <reversibility rating="costly">The allowlist schema and violation output become a repository-wide build contract; changing them can reject existing direct edges, so the policy must be derived from current metadata and covered by mutation tests.</reversibility>
  <files>xtask/src/arch.rs, xtask/allowlist.toml</files>
  <read_first>xtask/src/arch.rs; xtask/allowlist.toml; xtask/src/ci.rs; .github/workflows/ci.yml; xtask/src/main.rs; xtask/Cargo.toml; .planning/phases/01-foundations/01-RESEARCH.md; .planning/phases/01-foundations/01-PATTERNS.md; docs/02-architecture/decisions/ADR-0012-workspace-boundaries.md</read_first>
  <behavior>
    - A synthetic workspace with an explicitly allowed registry dependency passes, while the same dependency with a forbidden registry source fails with a deterministic crate/target/source violation.
    - A synthetic git dependency is rejected when its crate/source is not explicitly allowed, and a deliberately allowed git source passes only when the policy names it.
    - Existing workspace path rules remain fail-closed, and an unrecognized crate with any path, registry, or git edge is rejected.
  </behavior>
  <action>
    Extend the `cargo metadata` projection in `xtask/src/arch.rs` to retain the dependency source kind needed to distinguish path, registry, and git edges without changing the pure `violations(meta, allowlist)` shape. Add an explicit per-crate external policy under each `CrateRule`, with separate registry and git allow sets (or an equivalent typed representation that keeps those source classes distinct); an absent rule remains empty and fail closed. Preserve the existing `workspace.allow` checks and deterministic sorted diagnostics. Populate `xtask/allowlist.toml` from the current direct workspace-member metadata so every existing allowed registry edge remains legal, keep git allowances empty unless an existing tracked edge is explicitly justified, and do not add, install, or upgrade a package. Put synthetic metadata cases for allowed registry, forbidden registry, allowed git, forbidden git, unknown crate, and mixed path/external edges in the existing inline `xtask/src/arch.rs` test module; assert both the violation presence and stable message content. Keep the regression cases in the existing inline `xtask/src/arch.rs` test module; the project has no separate xtask integration-test target. Keep `cargo deny` as a separate supply-chain gate and retain the existing `cargo run -p xtask -- arch-check` CI step. Do not weaken the current path mutation test or treat a missing source field as an allowed external edge.
  </action>
  <verify>
    <automated>cargo test -p xtask && cargo run -q -p xtask -- arch-check</automated>
    <fails_when>either command exits non-zero, the inline registry/git mutation cases are absent, a forbidden registry or git edge is accepted, an existing allowed edge becomes forbidden because the policy was not derived from metadata, a missing crate rule is permissive, or the checked-in allowlist no longer parses.</fails_when>
  </verify>
  <acceptance_criteria>
    - `arch-check` evaluates registry and git sources separately from workspace/path sources and reports a non-zero result for an unapproved external edge.
    - The checked-in external policy accounts for current direct workspace-member dependencies without introducing a package or version change.
    - Inline tests in `xtask/src/arch.rs` prove allowed and forbidden registry/git cases and retain path fail-closed behavior; no untracked integration-test target is required.
    - The existing CI arch-check invocation remains the enforcement path and the local gate is green.
  </acceptance_criteria>
  <done>Forbidden registry and git dependency edges fail closed under an explicit checked-in policy, with current allowed edges and CI enforcement intact (D-02, D-03).</done>
</task>

<task type="auto" tdd="true">
  <name>TASK-001 / 01-05-02: Close the cross-phase task record with linked evidence</name>
  <reversibility rating="reversible">This task moves and updates documentation ledgers after implementation; the lifecycle transition can be reversed without changing source behavior, but completion must remain evidence-linked and append-only.</reversibility>
  <files>docs/04-tasks/active/TASK-001-foundation-gap-closure.md, docs/04-tasks/active/README.md, docs/04-tasks/completed/TASK-001-foundation-gap-closure.md, docs/04-tasks/completed/README.md</files>
  <read_first>docs/04-tasks/README.md; docs/04-tasks/active/README.md; docs/04-tasks/completed/README.md; docs/06-progress/task-done-rollup.md; .planning/phases/01-foundations/01-CONTEXT.md; .planning/phases/01-foundations/01-VALIDATION.md; .planning/phases/01-foundations/01-01-SUMMARY.md; .planning/phases/01-foundations/01-02-SUMMARY.md; .planning/phases/01-foundations/01-03-SUMMARY.md; .planning/phases/01-foundations/01-04-SUMMARY.md; .agent/definition-of-done.md; AGENTS.md</read_first>
  <behavior>
    - The active cross-phase stub exists before closure, then one task-owned filesystem/index transaction moves it to `docs/04-tasks/completed/`, removes its active-index entry, and adds its completed-index entry with the same task id and dated evidence.
    - The completed task record links all five plans, all D-01 through D-16 decision references, the five roadmap criteria, exact test commands, and any accepted deviations without claiming deferred work.
    - The active index has zero TASK-001-foundation-gap-closure references after the move, the completed index has exactly one, and every unrelated active/completed index line is byte-for-byte unchanged.
    - The final gate evidence is captured before the lifecycle transition; the post-transition rollup/status/validation/changelog updates remain owned by 01-05-03.
  </behavior>
  <action>
    Do not close the task until the preceding plan summaries and final gate results exist. First run the final gate and retain its command/exit evidence; only after that gate is green, perform the sole active-to-completed lifecycle update as one task-owned operation: save byte snapshots of `docs/04-tasks/active/README.md` and `docs/04-tasks/completed/README.md` outside the repository; remove exactly the existing TASK-001-foundation-gap-closure line from the active index; add exactly one dated `TASK-001-foundation-gap-closure` link to the completed index; move `docs/04-tasks/active/TASK-001-foundation-gap-closure.md` to `docs/04-tasks/completed/TASK-001-foundation-gap-closure.md` without changing its task id or ownership; and prove that filtering only the TASK-001 line from each before-snapshot equals the corresponding after-file. Do not rewrite, reorder, or reformat any unrelated index entry. Add a dated completion section in the moved record that links plans `01-01` through `01-05`, maps C1 through C5, the five roadmap success criteria, to repeatable commands, records the exact `cargo`, `xtask`, and CLI evidence, and explicitly lists the locked decisions D-01 through D-16. Record the brownfield reuse ledger, all ten named spec-less probes and their dispositions, the descriptor-less prohibitions, the absence of a new migration/package/broker, and any remaining owner-gated or deferred item without marking it complete. Leave `docs/06-progress/task-done-rollup.md` for the post-move consistency task 01-05-03; do not edit any unrelated Phase 2 worktree path or old ledger entry. This task is the only owner of the move and index handoff; 01-05-03 may rerun gates and update post-move records but must not move the task or change either index.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo run -q -p xtask -- arch-check && cargo run -q -p xtask -- migrate-check && cargo run -p xtask -- ci && test -f docs/04-tasks/completed/TASK-001-foundation-gap-closure.md && test ! -e docs/04-tasks/active/TASK-001-foundation-gap-closure.md && test "$(grep -F -c 'TASK-001-foundation-gap-closure' docs/04-tasks/active/README.md)" -eq 0 && test "$(grep -F -c 'TASK-001-foundation-gap-closure' docs/04-tasks/completed/README.md)" -eq 1 && grep -Fq 'Status: Completed' docs/04-tasks/completed/TASK-001-foundation-gap-closure.md && git diff --check -- docs/04-tasks</automated>
    <fails_when>the command exits non-zero, the move occurred before captured green final-gate evidence, either task path or the active/completed index disagree, active retains one TASK-001 entry, completed does not contain exactly one, the moved record is not marked `Status: Completed`, or the index diff touches unrelated entries.</fails_when>
  </verify>
  <acceptance_criteria>
    - The completed stub has one dated entry, links all five plans, cites D-01 through D-16, and records exact repeatable evidence.
    - The active directory no longer contains the closed task; the active README contains zero TASK-001-foundation-gap-closure entries; and the completed README contains exactly one.
    - Before/after index snapshots differ only by removal of the TASK-001 active-index line and addition of the dated completed-index entry; the task-done rollup remains unchanged until 01-05-03.
    - The record distinguishes automated completion from owner sign-off and preserves deferred/owner-gated items.
    - The final gate output is captured before the active-to-completed lifecycle transition; the task-file move plus active-index removal and completed-index addition occur as one post-gate task action, and task-done rollup/status/validation/changelog updates follow in 01-05-03.
  </acceptance_criteria>
  <done>TASK-001 is closed with auditable links to implementation plans, locked decisions, roadmap criteria, final evidence, and consistent active/completed indexes (D-01, D-02, D-03).</done>
</task>

<task type="auto">
  <name>TASK-001 / 01-05-03: Run the final Phase 1 gate and verify validation/progress records</name>
  <reversibility rating="reversible">These are verification records and a changelog entry; they can be corrected by append, but they must not be written until the implementation, architecture, and active-to-completed lifecycle gates are green.</reversibility>
  <files>scripts/verify-phase1-records.sh, .planning/phases/01-foundations/01-VALIDATION.md, docs/06-progress/task-done-rollup.md, docs/06-progress/status.md, CHANGELOG.md</files>
  <read_first>.planning/phases/01-foundations/01-VALIDATION.md; .planning/phases/01-foundations/01-01-SUMMARY.md; .planning/phases/01-foundations/01-02-SUMMARY.md; .planning/phases/01-foundations/01-03-SUMMARY.md; .planning/phases/01-foundations/01-04-SUMMARY.md; .planning/ROADMAP.md; .planning/REQUIREMENTS.md; docs/04-tasks/active/README.md; docs/04-tasks/completed/README.md; docs/04-tasks/completed/TASK-001-foundation-gap-closure.md; docs/06-progress/status.md; docs/06-progress/task-done-rollup.md; CHANGELOG.md; .agent/definition-of-done.md; .github/workflows/ci.yml</read_first>
  <behavior>
    - Formatting, clippy, focused phase tests, full workspace tests, architecture, migration, and the repository CI wrapper are run in the prescribed order and their actual outcomes are recorded.
    - The validation matrix maps all five roadmap criteria and all seven Phase 1 requirements to at least one automated command or an explicitly recorded human/owner boundary.
    - 01-05-02 has already captured the pre-transition final gate evidence and performed the sole task-file/index handoff; 01-05-03 updates validation, task-done rollup, status, and changelog records after that transition without moving the task or changing either index.
    - The final cross-record check proves each required token and status in each designated record independently; a token present in one record cannot satisfy another record's requirement.
  </behavior>
  <action>
    Run the final gate from the repository root in this order: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, the Phase 1 focused suites named in Plans 01-01 through 01-04 and in this Plan 05 task, `cargo test --workspace`, `cargo run -q -p xtask -- arch-check`, `cargo run -q -p xtask -- migrate-check`, and `cargo run -p xtask -- ci`. Do not read an 01-05 summary as an input; it does not exist until finalization after this task. Update `.planning/phases/01-foundations/01-VALIDATION.md` with the actual task/wave/threat/test matrix, set `wave_0_complete: true` and `nyquist_compliant: true` only when every required automated reference is covered, and record any local tool limitation (including the already documented cargo-deny situation) without converting it into a false pass. Append one newest-first Phase 1 entry to `docs/06-progress/task-done-rollup.md`, add a dated Phase 1 closure section to `docs/06-progress/status.md` while leaving the current Phase 2 pointer and unrelated gates intact, and add a concise `[Unreleased]` changelog entry describing the brownfield foundation closure, its evidence commands, and the explicit non-expansion boundary. Link C1 through C5, D-01 through D-16, TASK-001, and the completed plan summaries; do not mark owner sign-off, dataset licensing, remote deployment, or later-phase work complete.

    Create the dependency-free POSIX shell checker `scripts/verify-phase1-records.sh` in this task and run it after updating the four records. Treat this as a validation artifact, not a runtime product dependency. The script must first require every input file, then assert exact record-specific contracts: (a) validation independently contains every task ID `01-01-01` through `01-05-03`, all seven `REQ-*` IDs, C1–C5, D-01–D-16, PROBE-01–PROBE-10, every T-01 through T-05 threat predicate, and `nyquist_compliant: true` plus `wave_0_complete: true`; (b) the completed task contains `TASK-001-foundation-gap-closure`, `Status: Completed`, all five plan IDs, all seven requirement IDs, C1–C5, D-01–D-16, PROBE-01–PROBE-10, and all threat predicates; (c) the newest Phase 1 rollup section independently contains TASK-001, all five plan IDs, C1–C5, D-01–D-16, and the final gate command family; (d) the dated status section independently contains TASK-001, all five plan IDs, C1–C5, D-01–D-16, and `Status: Completed`; and (e) the Unreleased entry independently contains TASK-001, all five plan IDs, C1–C5, D-01–D-16, and the brownfield/non-expansion boundary. The script must also assert the active task path is absent, the completed task path is present, the active index count is exactly zero, the completed index count is exactly one, and the active/completed task status agree. Implement the script with a `require_tokens <file> <label> <tokens...>` helper that loops over one named file only and propagates missing-file failures; do not search a union of files, do not use `rg` across records, and do not suppress command errors. Update validation, rollup, status, and changelog only after this check passes, then run it again. Do not stage or edit any unrelated worktree path identified by the final 01-01 baseline.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo run -q -p xtask -- arch-check && cargo run -q -p xtask -- migrate-check && cargo run -p xtask -- ci && sh scripts/verify-phase1-records.sh && git diff --check -- scripts/verify-phase1-records.sh .planning/phases/01-foundations/01-VALIDATION.md docs/04-tasks docs/06-progress/task-done-rollup.md docs/06-progress/status.md CHANGELOG.md</automated>
    <fails_when>any required command exits non-zero, the record-specific checker is absent, any designated record lacks its own required token/status, active/completed lifecycle counts disagree, a roadmap criterion lacks a command/evidence link, the validation matrix claims a missing test as green, the status/changelog entries disagree, or the unrelated Phase 2 worktree file is modified.</fails_when>
  </verify>
  <acceptance_criteria>
    - The final validation matrix covers plans 01-01 through 01-05, all seven Phase 1 requirement IDs, all five roadmap criteria, and every security threat predicate introduced by the plans.
    - The checked-in `scripts/verify-phase1-records.sh` independently verifies validation, completed task, newest rollup section, status section, and Unreleased changelog contents; it never treats a token in one file as evidence for another and proves active/completed path, index, and status agreement.
    - The final validation matrix records the active TASK-001 creation gate, the post-gate active-to-completed task/index handoff, and the post-transition rollup/status/changelog updates in that order.
    - Full workspace tests, formatting, clippy, architecture, migration, and the repository CI wrapper have recorded actual outcomes; any unavailable optional local tool is labeled rather than hidden.
    - `docs/06-progress/status.md`, `task-done-rollup.md`, the completed task stub, `.planning/phases/01-foundations/01-VALIDATION.md`, and `CHANGELOG.md` tell the same completion story.
    - The final records state that Phase 1 is a brownfield gap closure and preserve all deferred/owner-gated boundaries.
    - The final status and changelog explicitly state that all unrelated worktree paths observed during execution remain outside the Phase 1 diff.
  </acceptance_criteria>
  <done>Phase 1 has a repeatable five-criterion evidence record, green repository gates, consistent task/progress documentation, and no unrelated or deferred scope changes (D-01 through D-16).</done>
</task>

</tasks>

## Artifacts this phase produces

- External dependency policy and pure classification logic in `xtask/src/arch.rs` and `xtask/allowlist.toml` — registry/git edges are explicit and fail closed (D-02, D-03).
- Inline tests in `xtask/src/arch.rs` — allowed/forbidden registry and git mutation coverage, including the CI-gated path case.
- `docs/04-tasks/completed/TASK-001-foundation-gap-closure.md`, active README removal, and completed README entry — closed cross-phase task record with consistent indexes.
- `docs/06-progress/task-done-rollup.md`, `docs/06-progress/status.md`, and `CHANGELOG.md` — consistent final progress and release evidence.
- `.planning/phases/01-foundations/01-VALIDATION.md` and `scripts/verify-phase1-records.sh` — completed Nyquist matrix plus deterministic record-specific closure checks.
- Final source audit: all four source types are covered; deferred ideas and any unrelated Phase 2 worktree modifications are explicitly excluded.
- Threat predicates introduced here: `T-05-ARCH`, `T-05-EXTERNAL`, `T-05-SUPPLYCHAIN`, `T-05-STATUS`, `T-05-SCOPE`, `T-05-INFO`; prohibition predicates: no unapproved external edge, no unverified completion, no package install, and no unrelated scope edit.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Cargo metadata → architecture policy | Workspace and external dependency declarations enter the build gate. |
| Build/CI result → repository status records | Automated outcomes become completion, progress, and changelog claims. |
| Task/validation records → operator decisions | Evidence links and unresolved probes guide what may be considered complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-ARCH | Tampering / elevation of privilege | `xtask::arch::violations` | critical | mitigate | Evaluate path, registry, and git source classes against explicit per-crate rules; retain deterministic synthetic path mutation tests and CI execution. |
| T-05-EXTERNAL | Tampering / supply-chain | registry/git dependency policy | high | mitigate | Reject unlisted external source/name pairs, derive existing allowances from metadata, and add forbidden-source mutation fixtures. |
| T-05-SUPPLYCHAIN | Tampering | package/build tooling | high | mitigate | Add no package or install task; keep `cargo-deny` as a separate CI gate and fail closed on unapproved edges. |
| T-05-STATUS | Repudiation | TASK-001, validation, rollup, status | high | mitigate | Require dated evidence links, exact record-specific verification, and full gate outputs before the active-to-completed task/index handoff. |
| T-05-SCOPE | Elevation of privilege | phase boundary and worktree | medium | mitigate | Audit all source types, preserve deferred/owner-gated items, and explicitly exclude the unrelated Phase 2 modification. |
| T-05-INFO | Information disclosure | final logs/status | medium | mitigate | Record command names and outcomes rather than raw payloads/secrets; reuse existing redaction for any generated diagnostic. |

</threat_model>

<verification>
Before declaring this plan complete, run these checks in order:

- `cargo test -p xtask` proves inline external policy and path regressions.
- `cargo run -q -p xtask -- arch-check && cargo run -q -p xtask -- migrate-check` proves the checked-in dependency and migration contracts.
- The Phase 1 focused commands in plans 01-01 through 01-04 prove the five user-visible criteria before the final workspace gate.
- `cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace` proves repository-wide correctness.
- `cargo run -p xtask -- ci` proves the project wrapper, with optional local tool limitations recorded rather than concealed.
- `sh scripts/verify-phase1-records.sh` independently proves each record's exact required tokens/status and the active/completed lifecycle counts.
- Inspect the final diff to confirm only planning/task/progress/architecture/verification-script artifacts changed and every unrelated worktree path remains unstaged.
</verification>

<success_criteria>
- A synthetic forbidden registry or git dependency fails `arch-check`, while current allowed edges remain green.
- The Phase 1 task record, validation matrix, progress rollup/status, and changelog all point to the same evidence-backed closure.
- All five roadmap criteria and seven requirement IDs have concrete automated or explicitly bounded human evidence.
- Full repository quality gates pass, or an unavailable optional local tool is recorded as such without a false completion claim.
- No deferred idea, later-phase feature, external package, broker, migration rewrite, or unrelated worktree change is included; all such paths are excluded by the 01-01 preservation baseline.
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-05-SUMMARY.md` only during finalization after task 01-05-03 has run. Record the external policy representation, mutation tests, exact final gate outcomes, completed TASK-001 path, record-specific verifier result, and any owner-gated or deferred follow-ups.
</output>
