---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/lib.rs
  - crates/cli/src/doctor.rs
  - crates/config/src/lib.rs
  - crates/application/src/db.rs
  - crates/application/src/audit_bridge.rs
  - crates/application/src/quran_cli.rs
  - crates/cli/tests/foundation.rs
  - crates/testkit/tests/config_precedence.rs
  - docs/04-tasks/active/TASK-001-foundation-gap-closure.md
files_deleted: []
autonomous: true
requirements:
  - REQ-product-vision
  - REQ-product-principles
  - REQ-goals-non-goals
  - REQ-engineering-baseline
  - REQ-storage-architecture
  - REQ-cli-api
  - REQ-architecture-principles-quality
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A fresh operator workspace can run `qai --help` and sees exactly the stable Phase 1 foundation groups: config, db, doctor, job, audit, secret, and source (D-08)."
    - "Effective configuration output shows the value selected by CLI > environment > file > defaults, includes the ValueOrigin for each leaf, and redacts secret values before human or JSON emission (D-07)."
    - "An absent, pending, checksum-mismatched, or current SQLite database has a distinct read-only readiness result; ordinary commands do not create or migrate the database, while `qai db migrate` remains the explicit creation/schema mutation path (D-05)."
    - "`qai doctor` creates no data directory, object directory, database, WAL, or probe file; it reports configuration, storage, migration, audit, and job readiness with exact remedies (D-06)."
    - "The same persisted hash-chain verifier is used by `qai audit verify` and doctor's `audit.chain_valid`; a gap or tampered sequence cannot be reported as a valid chain (D-12)."
    - "Adjacent configuration layers never merge values implicitly: the highest-precedence layer wins deterministically and its origin remains inspectable."
    - "Missing configuration, empty job catalogs, and an empty audit chain produce typed read-only results without phantom rows or false success."
    - "Origin explanations, audit events, and job catalogs have deterministic ordering when records compare equal."
    - statement: "Configuration key comparison and serialization use the stable dotted-key and UTF-8 representation defined by Config and OriginMap; alternate normalization or encoding does not change the reported origin."
      verification: backstop
  artifacts:
    - path: "crates/cli/tests/foundation.rs"
      provides: "Real-binary fresh-workspace readiness, help, config-origin, migration-remedy, doctor-no-side-effect, and audit-verification acceptance coverage"
    - path: "crates/cli/src/lib.rs"
      provides: "Loaded configuration/origin dispatch and stable foundation command tree"
    - path: "crates/application/src/db.rs"
      provides: "Shared existing-database readiness and migration status"
    - path: "crates/application/src/audit_bridge.rs"
      provides: "Persisted audit verification result consumed by CLI readiness checks"
    - path: "crates/application/src/quran_cli.rs"
      provides: "Existing-database guard for ordinary application reads"
    - path: "docs/04-tasks/active/TASK-001-foundation-gap-closure.md"
      provides: "Repository task record linked to GSD tasks 01-01-01 through 01-04-01"
  key_links:
    - from: "crates/cli/src/lib.rs"
      to: "crates/config/src/lib.rs"
      via: "dispatch calls Config::load once and passes Config plus OriginMap to handlers"
      pattern: "Config::load|OriginMap"
    - from: "crates/cli/src/doctor.rs"
      to: "crates/application/src/audit_bridge.rs"
      via: "doctor consumes verify_persisted_audit rather than inferring validity from row count"
      pattern: "verify_persisted_audit|audit_chain_valid"
    - from: "crates/application/src/quran_cli.rs"
      to: "crates/application/src/db.rs"
      via: "ordinary opens call the existing-database readiness guard before any read path"
      pattern: "readiness|open_read_only|MigrationRequired"
    - from: "crates/cli/tests/foundation.rs"
      to: "crates/cli/src/lib.rs"
      via: "the real qai binary exercises the public command tree and exit codes"
      pattern: "CARGO_BIN_EXE_qai|--help|config show|doctor"
  assumptions:
    - requirement_id: "REQ-product-vision"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      probe: "unclassified — review manually"
      coverage_note: "Plan actions and the source audit keep the change on foundation seams only; this assumption remains for verifier review."
    - requirement_id: "REQ-goals-non-goals"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      probe: "unclassified — review manually"
      coverage_note: "No model, remote provider, sectarian-authority, or corpus feature is introduced by this plan; verifier must confirm the boundary."
    - requirement_id: "REQ-engineering-baseline"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      probe: "unclassified — review manually"
      coverage_note: "Focused tests plus repository gates are planned, but the spec-less probe cannot infer the complete engineering contract."
    - requirement_id: "REQ-storage-architecture"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      probe: "unclassified — review manually"
      coverage_note: "The existing SQLite/UnitOfWork primitives are reused; verifier must confirm all storage boundaries remain portable."
    - requirement_id: "REQ-cli-api"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      probe: "unclassified — review manually"
      coverage_note: "The stable foundation groups and redacted JSON are exercised by the real binary, while the broader API contract is outside this phase."
    - requirement_id: "REQ-architecture-principles-quality"
      category: "unclassified"
      status: "unresolved"
      flagged: true
      probe: "unclassified — review manually"
      coverage_note: "Arch enforcement is assigned to 01-05; this assumption is retained until the final gate verifies it."
  prohibitions:
    - statement: "MUST NOT expose secret values in effective config, doctor, job, or audit output."
      status: "unresolved"
      verification: null
      reason: "Spec-less prohibition fallback: retain as flagged-unverified; the locked ADR-0005 and redaction invariants still govern implementation."
    - statement: "MUST NOT create or modify files, directories, or database state from a doctor diagnostic."
      status: "unresolved"
      verification: null
      reason: "Spec-less prohibition fallback: retain as flagged-unverified; D-06 and project coding rules make this a trust invariant."
    - statement: "MUST NOT apply migrations or start a worker implicitly from an ordinary one-shot command."
      status: "unresolved"
      verification: null
      reason: "Spec-less prohibition fallback: retain as flagged-unverified; D-05 and D-13 govern operator-visible execution ownership."
    - statement: "MUST NOT report an audit chain as valid from row count or structural presence alone."
      status: "unresolved"
      verification: null
      reason: "Spec-less prohibition fallback: retain as flagged-unverified; D-12 requires persisted hash and sequence verification."
---

<objective>
Close the evidence-backed operator-bootstrap gaps FND-01, FND-02, and FND-03 with one production-quality fresh-workspace readiness tracer, then expand it into complete effective-configuration inspection and non-mutating database readiness. This plan implements D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, and D-12. Reuse `Config::load`, `OriginMap`, `SqliteDatabase::open_read_only`, the checksummed migration runner, `verify_persisted_audit`, and the existing Clap tree; do not create a parallel configuration, diagnostic, or storage service.

Purpose: Make the first operator journey truthful before any mutation surface is exposed: inspect effective values and origins, diagnose a fresh workspace without creating state, migrate only through the explicit command, and verify the persisted audit chain through the same authority as the CLI.

Output: A real-binary foundation acceptance test plus the smallest CLI/application seam changes that make configuration precedence, explicit migration remedies, stable help groups, and read-only doctor/audit readiness observable.
</objective>

<execution_context>
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/workflows/execute-plan.md
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-PATTERNS.md
@.planning/phases/01-foundations/01-VALIDATION.md
@AGENTS.md
@.agent/instructions.md
@.agent/coding-rules.md
@docs/02-architecture/decisions/ADR-0002-migration-strategy.md
@docs/02-architecture/decisions/ADR-0004-config-precedence.md
@docs/02-architecture/decisions/ADR-0005-secret-storage.md
@docs/02-architecture/decisions/ADR-0009-audit-integrity.md
@docs/02-architecture/decisions/ADR-0010-error-taxonomy.md
@crates/config/src/lib.rs
@crates/config/src/origin.rs
@crates/cli/src/lib.rs
@crates/cli/src/doctor.rs
@crates/application/src/db.rs
@crates/application/src/audit_bridge.rs
@crates/application/src/quran_cli.rs
@crates/cli/tests/catalog.rs
@crates/testkit/tests/config_precedence.rs
</context>

## Reuse ledger

| Current seam | Demonstrated gap | Smallest closing change |
|---|---|---|
| `Config::load` + `OriginMap` | CLI handlers discard the origin pair and reconstruct `Config::default()` | Load one effective pair at dispatch and pass it through |
| `SqliteDatabase::open_read_only` | doctor and ordinary application opens can still reach creating constructors | Add one readiness guard and route ordinary opens through it |
| `verify_persisted_audit` | doctor checks only `probe.audit_events` count | Carry the persisted verifier result into `DbProbe`/doctor |
| Clap `Commands` | no focused contract for the seven foundation groups | Add a real-binary help assertion without renaming groups |
| `migrate::apply_migrations` | ordinary commands can create/mutate implicitly | Keep it reachable only from `qai db migrate`; report missing/pending states elsewhere |

## Source audit

| SOURCE | ID | Feature or contract | Plan/task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | G-01 | Workspace builds and `qai --help` exposes stable groups | 01-01/01-04-01 | COVERED | Reused Clap tree, focused real-binary test |
| GOAL | G-02 | CLI > env > file > defaults with actionable validation | 01-01/01-01-01, 01-01-01-02 | COVERED | Existing `Config::load` is made observable |
| GOAL | G-03 | Provenance and append-only audit for state changes | 01-02 and 01-03 | COVERED | Doctor verifier and transactional writes are split across the dependency chain |
| GOAL | G-04 | Durable jobs enqueue, lease, checkpoint, and cancel | 01-03 and 01-04 | COVERED | SQLite queue remains authority; host is added without a broker |
| GOAL | G-05 | Architecture check and CI reject forbidden dependencies | 01-05 | COVERED | External source policy and CI mutation coverage |
| REQ | REQ-product-vision | Trustworthy Quran-first foundation boundary | 01-01, 01-04 | COVERED | No later corpus/search/server capability is added |
| REQ | REQ-product-principles | Traceability, redaction, locality, fail-closed behavior | 01-01 through 01-05 | COVERED | Read-only, redacted, audited paths are explicit |
| REQ | REQ-goals-non-goals | No foundation model, vector service, remote provider, or sectarian authority claim | 01-01, 01-05 | COVERED | Scope and dependency gates preserve the boundary |
| REQ | REQ-engineering-baseline | Typed errors, validated config, real tests, DX, cancellation, observability | 01-01 through 01-05 | COVERED | Existing commands and focused acceptance tests are reused |
| REQ | REQ-storage-architecture | SQLite authority, portable ports, explicit migrations, outbox | 01-01 and 01-02 | COVERED | No replacement store; UnitOfWork remains the boundary |
| REQ | REQ-cli-api | Executable `qai`, stable groups, JSON, remedies, cancellable operations | 01-01, 01-03, 01-04 | COVERED | Real binary tests and existing exit mapper |
| REQ | REQ-architecture-principles-quality | Correctness, maintainability, extensibility, enforced boundaries | 01-05 plus all plans | COVERED | Pure metadata gate and CI are retained |
| RESEARCH | FND-01 | Effective config/origins/keyed get/validation at CLI | 01-01-01-02 | COVERED | Reuses `Config::load`; no second merge |
| RESEARCH | FND-02 | Doctor filesystem side effects and row-count audit check | 01-01-01-01, 01-01-01-03 | COVERED | Metadata-only checks and persisted verifier |
| RESEARCH | FND-03 | Missing/pending migration and creating ordinary opens | 01-01-01-01, 01-01-01-03 | COVERED | Explicit `qai db migrate` remedy |
| RESEARCH | FND-04 | Fragmented domain/provenance/audit/outbox writes | 01-02 | COVERED | One same-UnitOfWork sequencing seam |
| RESEARCH | FND-05 | Memory-only checkpoints, global retry, cancel refusal | 01-03 | COVERED | Existing job columns and queue are extended |
| RESEARCH | FND-06 | Inline worker in one-shot import and absent host | 01-04-01-01, 01-04-01-02 | COVERED | `qai serve` owns the loop |
| RESEARCH | FND-07 | Path-only architecture enforcement | 01-05-01-01 | COVERED | External registry/git rules and mutation test |
| RESEARCH | ARCH-01 | Layered responsibility: CLI syntax, application composition, storage adapter, jobs backend-neutral | 01-01 through 01-05 | COVERED | No edge crate reaches `storage-sqlite` directly |
| RESEARCH | ARCH-02 | Explicit migration and metadata-only doctor | 01-01 | COVERED | Existing `open_read_only` and migration runner |
| RESEARCH | ARCH-03 | Long-lived worker host and backend-neutral jobs | 01-03, 01-04 | COVERED | Tokio/SQLite only |
| RESEARCH | ARCH-04 | Persisted audit verifier as doctor authority | 01-01, 01-02 | COVERED | Same verifier report is reused |
| RESEARCH | ARCH-05 | Machine-enforced workspace/external dependency policy | 01-05 | COVERED | Pure `violations` plus CI |
| RESEARCH | CONSTRAINT-01 | No new external package or broker; preserve SQLite | 01-01 through 01-05 | COVERED | Existing `Cargo.lock`, Tokio, SQLx, and SQLite only |
| RESEARCH | CONSTRAINT-02 | If schema change is unavoidable, append-only checksummed migration plus `xtask migrate-check` | 01-01, 01-03, 01-05 | COVERED | This plan uses existing columns; no migration is planned |
| RESEARCH | CONSTRAINT-03 | Package legitimacy gate is not applicable because no install is proposed | 01-01 through 01-05 | COVERED | No package action is emitted |
| CONTEXT | D-01 | Evidence-driven brownfield gap closure | 01-01 objective and all task actions | COVERED | Reuse ledger identifies each gap |
| CONTEXT | D-02 | Roadmap and ADRs 0000–0012 govern completion | 01-01 objective and context | COVERED | No locked decision is reopened |
| CONTEXT | D-03 | Repeatable automated/CLI evidence closes criteria | 01-01-01-01, 01-05-01-01 | COVERED | Real-binary and build-gate checks |
| CONTEXT | D-04 | Reuse later Quran/search/server code only as evidence | 01-01-01-03, 01-04-01-02 | COVERED | Only existing open/worker boundaries are touched |
| CONTEXT | D-05 | Migrations remain explicit | 01-01-01-01, 01-01-01-03 | COVERED | Ordinary readiness never applies schema |
| CONTEXT | D-06 | Doctor is read-only first-run diagnosis | 01-01-01-01, 01-01-01-03 | COVERED | No probe writes |
| CONTEXT | D-07 | Redacted effective config with origins and JSON | 01-01-01-02 | COVERED | Redaction happens at emission |
| CONTEXT | D-08 | Stable foundation command groups | 01-01-01-01, 01-01-01-02 | COVERED | Help contract test |
| CONTEXT | D-09 | Audit authoritative source/approval/import changes | 01-02 | COVERED | One transaction composition |
| CONTEXT | D-10 | Domain/provenance/audit/outbox all-or-nothing | 01-02 | COVERED | Failure injection at each boundary |
| CONTEXT | D-11 | Job lifecycle audit transitions | 01-03-01-03 | COVERED | Same-UoW queue transitions |
| CONTEXT | D-12 | `qai audit verify` is authoritative | 01-01-01-01, 01-01-01-03 | COVERED | Doctor delegates to persisted verifier |
| CONTEXT | D-13 | Long-lived hosts run workers; one-shot commands enqueue | 01-04-01-01, 01-04-01-02 | COVERED | Host/one-shot boundary |
| CONTEXT | D-14 | Named durable checkpoints and idempotent resume | 01-03-01-01, 01-03-01-02 | COVERED | Existing checkpoint JSON stores named payloads |
| CONTEXT | D-15 | Bounded per-kind retry with explicit retry | 01-03-01-01, 01-03-01-02, 01-03-01-03 | COVERED | Policy keyed by handler kind |
| CONTEXT | D-16 | Cooperative cancellation reports boundary outcome | 01-03-01-02, 01-03-01-03 | COVERED | Three terminal outcomes are persisted |

All source rows are covered. Deferred ideas in `01-CONTEXT.md` and research items explicitly marked out of scope are not included in any task.

## Spec-less probe disposition

The deterministic report contains ten items. The four `REQ-product-principles` edge rows are resolved above with explicit/backstop truths. The six `unclassified` rows remain explicit flagged assumptions in `must_haves.assumptions`; none is silently converted into a pass. The prohibition items are descriptor-less and unresolved so the verifier cannot treat them as mechanically enforced by the plan alone.

<tasks>

<task type="tracer">
  <name>TASK-001 / 01-01-01: Fresh-workspace operator readiness flow</name>
  <precondition>The tracked Rust workspace, `migrations/sqlite/`, and `Cargo.lock` are present at the repository root.</precondition>
  <reversibility rating="costly">The loaded-config envelope and readiness result cross the CLI/application boundary and change operator-facing output, but they do not alter stored data or published wire formats.</reversibility>
  <files>crates/cli/src/lib.rs, crates/application/src/db.rs, crates/application/src/audit_bridge.rs, crates/cli/src/doctor.rs, crates/cli/tests/foundation.rs</files>
  <read_first>crates/cli/src/lib.rs; crates/application/src/db.rs; crates/application/src/audit_bridge.rs; crates/cli/src/doctor.rs; crates/cli/tests/catalog.rs; crates/cli/tests/foundation.rs; docs/02-architecture/decisions/ADR-0002-migration-strategy.md; docs/02-architecture/decisions/ADR-0004-config-precedence.md; docs/02-architecture/decisions/ADR-0009-audit-integrity.md; .planning/phases/01-foundations/01-CONTEXT.md; .planning/phases/01-foundations/01-PATTERNS.md</read_first>
  <behavior>
    - Fresh path: `qai --help` shows all seven stable groups; `qai config show --json` reports the loaded file/env value and origin; `qai doctor --json` reports an actionable missing/pending database state without creating a path; `qai db migrate` then creates the schema; `qai audit verify` validates the empty chain.
    - Existing path: after migration, doctor consumes the persisted audit verifier and reports its valid/gap/tampered result rather than a row count.
  </behavior>
  <action>
    Wire the single operator path through the existing composition seams, without a second config or storage abstraction. Change `dispatch` to build a complete `BTreeMap` of global CLI overrides for `--data-dir` and `--log-level`, call `Config::load` once, and carry the returned `(Config, OriginMap)` into the config, doctor, and database handlers; preserve every current `Commands` variant and the seven foundation group names (D-01, D-02, D-03, D-04, D-08). Add one shared readiness result in `application::db` that distinguishes missing file, unreadable file, pending migration, checksum mismatch, and current schema, and route `qai db status`/ordinary opens through it. Keep `migrate::apply_migrations` behind `DbAction::Migrate` only. Extend `DbProbe` with the persisted audit verification result and make `doctor::audit_chain_valid` render that result, including `gaps` and `tampered_sequences`, instead of treating a positive row count as validity (D-05, D-06, D-12). In `crates/cli/tests/foundation.rs`, drive the real `CARGO_BIN_EXE_qai` with a fresh `tempfile::TempDir`, assert no database/WAL/object/probe path appears after doctor, assert the exact `qai db migrate` remedy, and assert the seven groups in help.
  </action>
  <verify>
    <automated>cargo test -p cli --test foundation -- --nocapture</automated>
    <fails_when>the command exits non-zero, the fresh-workspace assertion fails, any side-effect path is found, or the help/config/doctor/migration/audit flow does not match the expected exit and output.</fails_when>
  </verify>
  <acceptance_criteria>
    - The test invokes the real qai binary and observes `config`, `db`, `doctor`, `job`, `audit`, `secret`, and `source` in top-level help.
    - The test proves file and environment configuration reaches the effective output with an origin, while a secret sentinel is absent from stdout/stderr.
    - The test proves doctor leaves a fresh data directory empty and reports `qai db migrate` for a missing or pending database.
    - The test proves the empty post-migration audit chain verifies successfully and that the doctor audit check uses the same result shape.
  </acceptance_criteria>
  <done>The one fresh-workspace operator path builds and runs end to end, exposes stable foundation groups, reports effective config with origins, keeps doctor non-mutating, and routes schema creation through the explicit migration command.</done>
</task>

<task type="auto">
  <name>TASK-001 / 01-01-02: Complete effective configuration inspection</name>
  <reversibility rating="costly">The human and JSON config envelopes become a stable operator surface, but they remain local CLI output and can be versioned without a database migration.</reversibility>
  <files>crates/cli/src/lib.rs, crates/config/src/lib.rs, crates/testkit/tests/config_precedence.rs, crates/cli/tests/foundation.rs</files>
  <read_first>crates/cli/src/lib.rs; crates/config/src/lib.rs; crates/config/src/origin.rs; crates/testkit/tests/config_precedence.rs; crates/cli/tests/foundation.rs; docs/02-architecture/decisions/ADR-0004-config-precedence.md; docs/02-architecture/decisions/ADR-0005-secret-storage.md; docs/02-architecture/decisions/ADR-0010-error-taxonomy.md; .planning/phases/01-foundations/01-RESEARCH.md; .planning/phases/01-foundations/01-PATTERNS.md</read_first>
  <action>
    Close FND-01 at the CLI boundary while retaining `Config::load` as the only merge engine. Pass the loaded `OriginMap` into `handle_config_show`, `handle_config_get`, and `handle_config_validate`; never create `Config::default()` after a config file or environment value has been supplied. Expand `mark_defaults` so every serialized leaf is represented in the origin map, and keep `BTreeMap` ordering stable. For human output, render each effective key as `key = value` plus its origin when `--explain` is requested; for JSON, emit one envelope containing the effective configuration and the dotted-key origin map, then run `application::redaction::redact_json_value` before printing (D-07). Make `config get <key>` resolve dotted keys against the effective serialized value, return a typed not-found result for an unknown key, and print the key’s origin in explain-capable output. Make `config validate --file` load and validate the named file through `Config::load`; map malformed/unsafe values through the existing `ConfigError` and CLI validation exit rather than emitting an uncoded default. Ensure `--data-dir` updates the effective `app.data_dir`, `storage.sqlite.path`, and `storage.objects.root` and records the CLI origin for the derived storage keys. Add matrix tests for defaults, file, env, CLI, same-key adjacency, empty/missing input, and nested secret redaction. Do not add a new config file mutation command or a new package.
  </action>
  <verify>
    <automated>cargo test -p testkit --test config_precedence && cargo test -p cli --test foundation -- --nocapture</automated>
    <fails_when>either test command exits non-zero, precedence loses the documented order, an origin is missing for a serialized leaf, `config get` ignores its key, or a secret sentinel appears in output.</fails_when>
  </verify>
  <acceptance_criteria>
    - File value, environment value, and CLI value are exercised together, with the expected selected value and `ValueOrigin` variant in the result.
    - `config show --json` contains effective values and dotted-key origins, and redaction is applied before serialization reaches stdout.
    - `config get server.port` returns the requested value, not the full debug dump; unknown keys produce the existing not-found/validation contract.
    - `config validate` identifies the offending value and maps to the documented validation exit code.
  </acceptance_criteria>
  <done>The real CLI now exposes one effective, origin-bearing, redacted configuration for file/env/CLI/default precedence and actionable keyed validation.</done>
</task>

<task type="auto">
  <name>TASK-001 / 01-01-03: Enforce metadata-only doctor and existing-database guards</name>
  <reversibility rating="costly">The readiness guard touches shared application open paths and doctor semantics, but it changes no stored schema or canonical data and remains behind existing application ports.</reversibility>
  <files>crates/cli/src/doctor.rs, crates/application/src/db.rs, crates/application/src/quran_cli.rs, crates/cli/tests/foundation.rs</files>
  <read_first>crates/cli/src/doctor.rs; crates/application/src/db.rs; crates/application/src/quran_cli.rs; crates/storage-sqlite/src/lib.rs; crates/storage-sqlite/src/migrate.rs; crates/cli/src/doctor.rs; crates/cli/tests/foundation.rs; docs/02-architecture/decisions/ADR-0001-relational-store.md; docs/02-architecture/decisions/ADR-0002-migration-strategy.md; docs/02-architecture/decisions/ADR-0009-audit-integrity.md; .agent/coding-rules.md; .planning/phases/01-foundations/01-RESEARCH.md</read_first>
  <action>
    Replace the two filesystem probe writes in `data_dir_writable` and `filesystem_object_store_writable` with `symlink_metadata`/existence/permission inspection only. A missing path must produce an actionable warning or failure with a remedy, never `create_dir_all` or a temporary file. Make `application::db` return distinct readiness diagnostics for missing, unreadable, pending, checksum-mismatched, and current SQLite state, and use `SqliteDatabase::open_read_only` for ordinary read/list/status paths. Replace the creating `open_db` helper in `crates/application/src/quran_cli.rs` with an existing-database guard for ordinary commands; keep database creation and migration confined to the explicit application migration function reached by `qai db migrate`. Make doctor’s audit check consume the persisted verifier report and expose its `valid`, `gaps`, and `tampered_sequences` fields with `qai audit verify` as the next command. Add fresh-directory, pending/current migration, restricted-file, and tampered-audit cases to `crates/cli/tests/foundation.rs`; do not weaken the existing JSON schema or add a repair path that writes during doctor (D-05, D-06, D-12).
  </action>
  <verify>
    <automated>cargo test -p cli --test foundation -- --nocapture && cargo test -p application --lib db</automated>
    <fails_when>the commands exit non-zero, doctor creates any path or file, an ordinary read opens a creating SQLite constructor, a pending database lacks the exact migration remedy, or tampered audit rows still produce a passing doctor result.</fails_when>
  </verify>
  <acceptance_criteria>
    - A fresh data directory and an existing database are both checked without creating directories, databases, WAL files, or probe files.
    - Missing and pending states name `qai db migrate`; checksum mismatch names `qai db verify`; current state is distinguishable without a mutation.
    - Ordinary application reads use the existing-database guard and return a typed migration-required diagnostic.
    - A doctor run against a tampered persisted chain reports non-valid and points to `qai audit verify`.
  </acceptance_criteria>
  <done>Doctor and ordinary database inspection are metadata/read-only, migration mutation is explicit, and persisted audit verification is the single audit-readiness authority.</done>
</task>

</tasks>

## Artifacts this phase produces

- `crates/cli/tests/foundation.rs` — the real-binary tracer and expansion acceptance suite for the seven groups, precedence/origins/redaction, migration remedies, doctor no-side-effects, and audit verification.
- `LoadedConfig`/the loaded `(Config, OriginMap)` dispatch envelope in `crates/cli/src/lib.rs` — one effective configuration path for all foundation handlers (D-01, D-03, D-07, D-08).
- `DatabaseReadiness` and the existing-database guard in `crates/application/src/db.rs` — missing/pending/checksum/current states with `qai db migrate` remedies (D-05).
- Persisted audit verification carried through `DbProbe` into `crates/cli/src/doctor.rs` — no row-count trust and no diagnostic writes (D-06, D-12).
- `docs/04-tasks/active/TASK-001-foundation-gap-closure.md` — the repository task record that later plans append evidence to and the final plan moves to `docs/04-tasks/completed/`.
- Threat predicates introduced here: `T-01-CFG`, `T-01-DOCTOR`, `T-01-MIG`, `T-01-AUDIT`, and `T-01-SQL`; prohibition predicates: no secret output, no doctor writes, no implicit migration/worker, and no row-count audit pass.
- Decision predicates introduced here: D-01 through D-08 and D-12, with all cited in executable task content.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI/config file/environment → `Config::load` | Operator-controlled values enter the effective configuration and may carry secrets. |
| CLI/application → SQLite adapter | Read paths cross from a diagnostic/request context into a persistent store. |
| `doctor` → filesystem/audit tables | Diagnostics must observe metadata without changing either resource. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-CFG | Information disclosure | `Config::load`, config/doctor emission | high | mitigate | Serialize the effective envelope, apply the shared redaction layer before stdout/stderr, and run the sentinel regression in `crates/cli/tests/foundation.rs` and `crates/testkit/tests/config_precedence.rs`. |
| T-01-DOCTOR | Tampering / elevation of privilege | `crates/cli/src/doctor.rs` filesystem checks | high | mitigate | Replace write probes with `symlink_metadata`/permission inspection and assert a fresh directory remains empty. |
| T-01-MIG | Tampering | `application::db` and `quran_cli::open_db` | high | mitigate | Use `open_read_only` for ordinary paths, expose pending/missing states, and reserve `migrate_database` for `DbAction::Migrate`. |
| T-01-AUDIT | Repudiation / tampering | `doctor::audit_chain_valid` | high | mitigate | Reuse `verify_persisted_audit` and render sequence gaps/tampered hashes; never use row count as validity. |
| T-01-SQL | Tampering | catalog/status SQL | medium | mitigate | Preserve SQLx bound parameters for operator-provided ids and use only existing `PRAGMA`/catalog queries. |
</threat_model>

<verification>
Before declaring this plan complete, run these checks in order:

- `cargo test -p config --lib` and `cargo test -p testkit --test config_precedence` prove the existing merge contract remains green.
- `cargo test -p cli --test foundation --test catalog` proves the real binary and the seven-group contract.
- `cargo test -p application --lib db` proves readiness and no-side-effect behavior at the application boundary.
- `cargo run -q -p xtask -- arch-check` and `cargo run -q -p xtask -- migrate-check` prove the existing workspace and migration gates remain green.
</verification>

<success_criteria>
- The tracer test proves the operator can inspect, diagnose, explicitly migrate, and verify from a fresh workspace.
- Every non-passing readiness/validation result names an exact remedy and uses the centralized exit-code mapping.
- No secret sentinel appears in config, doctor, or JSON output.
- No doctor invocation creates or removes a filesystem/database object.
- The task record contains the commands, evidence, and the five roadmap criterion links for the later phase verifier.
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-01-SUMMARY.md` when done. The summary must record reused seams, the exact gap closed, test commands/results, any new symbols/files, and the repository `TASK-001` status update.
</output>
