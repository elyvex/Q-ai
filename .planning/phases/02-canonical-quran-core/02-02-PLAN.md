---
phase: 02-canonical-quran-core
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - fixtures/quran/test-edition-rich/manifest.json
  - fixtures/quran/test-edition-rich/ayahs.csv
  - fixtures/quran/test-edition-rich/reference.json
  - fixtures/quran/test-edition-identity/manifest.json
  - fixtures/quran/golden/ayah_texts.jsonl
  - crates/quran-corpus/tests/fixtures.rs
autonomous: true
requirements:
  - REQ-quran-corpus
  - REQ-ingestion-validation-eval
user_setup: []
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A richer synthetic edition (`fixtures/quran/test-edition-rich/`) exercises more surahs/ayahs, a basmala inside an ayah body, pause marks U+06D6…U+06ED, a mark-separated token, a long ayah, page/division boundaries, a muqattaʿat ayah, a repeated refrain, and U+0670, while remaining `synthetic: true` and never canonical (D-08, D-05/ADR-0101 Option B; edge probe 'encoding': the fixture declares NFC storage and is asserted over its stored bytes)."
    - "A companion synthetic reference edition (`test-edition-rich/reference.json`) is a valid `EditionSource` with a declared `reference_corpus_id` and `reference_text_hash` pin, marked `synthetic: true`, and names no real upstream dataset, publisher, or license (OD-01/OD-03 stay gated; edge probe 'empty input': a fixture with no real identity keeps every identity field absent rather than invented)."
    - "A small `test-edition-identity` manifest declares a non-null `upstream_edition_slug`, `qai_edition_id`, the primary/default flag, and a concrete license, so the identity/primary operator path (plan 02-03) has a real fixture; it is likewise `synthetic: true` and non-canonical."
    - "The CSV mirror reproduces the rich JSON ayahs field-for-field under the existing `CsvAdapter`, and the golden set carries one row per rich-fixture ayah; no duplicate `(surah, ayah)` id and dense per-surah numbering hold (edge probe 'unclassified' for REQ-data-separation-layers is answered by a fixture that is explicitly a non-canonical data layer, never a canonical slot)."
  artifacts:
    - "fixtures/quran/test-edition-rich/{manifest.json,ayahs.csv,reference.json} (new synthetic edition + companion reference)"
    - "fixtures/quran/test-edition-identity/manifest.json (identity/primary/license fixture for the operator path)"
    - "fixtures/quran/golden/ayah_texts.jsonl (extended)"
    - "crates/quran-corpus/tests/fixtures.rs (rich + identity shape, CSV-parity, and golden assertions)"
  key_links:
    - "fixtures/quran/test-edition-rich/manifest.json → JsonAdapter → EditionSource (schema-conformant, `synthetic: true`) → shared by plan 02-03's verify/reference snapshots and plan 02-07's integrity artifact"
    - "fixtures/quran/test-edition-rich/ayahs.csv → CsvAdapter → the same ayah set as the JSON manifest (field-for-field parity)"
    - "fixtures/quran/test-edition-identity/manifest.json → `qai quran import` → `EditionMeta.is_primary`/upstream identity → canonical row → `qai quran edition show` (plan 02-03)"
  prohibitions:
    - "No agent, model, or importer may generate, correct, or substitute canonical text; only verbatim manifest bytes are stored. [flagged-unverified]"
    - "The importer must never invent a dataset slug, publisher, release, license, reviewer, or reference-corpus identity; absent or owner-gated identity stays unset and is recorded as OD-01/OD-03. [flagged-unverified]"
    - "Synthetic fixtures are never canonical and every surface that shows one must label it non-canonical. [flagged-unverified]"
    - "Never present a QV-015 reference-comparison skip as a pass. [flagged-unverified]"
---

<objective>
Deepen the exercised dataset before the operator surfaces are built on it (D-08). This plan is the "fixture" half of the split of the former plan 02-02: it lands the richer synthetic edition, its companion synthetic reference, and the identity/primary/license fixture that plan 02-03's operator snapshots and plan 02-07's corpus-integrity artifact consume.

Task ledger: `TASK-002-canonical-quran-core` (see `docs/04-tasks/`) — this plan is one implementation unit of that phase task.

Purpose: "corpus integrity checks pass" is only meaningful if the corpus stresses the checks. The current `test-edition-min` is 5 surahs / 14 ayahs and does not exercise a basmala inside an ayah body, mark-separated tokens, page boundaries, muqattaʿat, refrains, or superscript alef. It also carries no upstream identity, primary flag, or declared license, so the identity/primary operator path has nothing to import.
Output: `fixtures/quran/test-edition-rich/{manifest.json,ayahs.csv,reference.json}`, `fixtures/quran/test-edition-identity/manifest.json`, an extended golden set, and fixture-shape tests — while `test-edition-min` and all 16 adversarial corpora stay byte-identical.
</objective>

<execution_context>
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/workflows/execute-plan.md
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-canonical-quran-core/02-CONTEXT.md
@.planning/phases/02-canonical-quran-core/02-RESEARCH.md
@.planning/phases/02-canonical-quran-core/02-PATTERNS.md
@fixtures/quran/test-edition-min/manifest.json
@crates/quran-corpus/tests/fixtures.rs
@crates/quran-corpus/src/tokenize.rs
@docs/02-architecture/decisions/ADR-0104-unicode-policy.md
@docs/02-architecture/decisions/ADR-0105-canonical-tokenization.md
@docs/schemas/quran-edition-source.v1.schema.json
@docs/04-tasks/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Richer synthetic edition fixture with CSV and golden mirrors</name>
  <files>fixtures/quran/test-edition-rich/manifest.json, fixtures/quran/test-edition-rich/ayahs.csv, fixtures/quran/golden/ayah_texts.jsonl, crates/quran-corpus/tests/fixtures.rs</files>
  <read_first>
    - fixtures/quran/test-edition-min/manifest.json (top-level `format`/`format_version`/`edition`/`expected`/`surahs`/`ayahs` shape, `synthetic: true`, basmala policies)
    - fixtures/quran/test-edition-min/ayahs.csv (CSV mirror shape that must reproduce the JSON ayahs)
    - fixtures/quran/golden/ayah_texts.jsonl (one row per ayah: surah, ayah, text)
    - crates/quran-corpus/tests/fixtures.rs (`test_edition_min_parses_with_expected_shape`, `csv_adapter_reproduces_the_same_ayahs`, `golden_ayah_texts_match_the_imported_fixture`, `all_sixteen_adversarial_manifests_are_format_valid`)
    - crates/quran-corpus/src/tokenize.rs (`is_pause_mark` branch driven by U+06D6…U+06ED signs)
    - docs/02-architecture/decisions/ADR-0104-unicode-policy.md and ADR-0105-canonical-tokenization.md
    - docs/schemas/quran-edition-source.v1.schema.json (the `is_primary`/`upstream_edition_slug`/`qai_edition_id` properties added in plan 02-01; `additionalProperties: false`)
  </read_first>
  <action>
    Add a richer synthetic edition under `fixtures/quran/test-edition-rich/` using the exact `test-edition-min` manifest shape (same `format`, `format_version`, `edition` block, `expected` block with `reference_corpus_id` and `reference_text_hash` pins, `surahs`, `ayahs`), with `edition.synthetic: true`, a distinct slug, and coverage that stresses all six integrity families. Per D-05 (ADR-0101 Option B) this phase ships no real canonical text: engineering runs exclusively against synthetic fixtures and an operator imports an approved edition, so the new fixture must remain `synthetic: true` and non-canonical, and nothing may treat it as the shipped dataset. Cover: dozens of ayahs spanning multiple juz/hizb/rub/manzil/ruku/page boundaries, a basmala occurring inside an ayah body (the An-Naml 27:30 shape), pause marks U+06D6…U+06ED, at least one token separated by a mark rather than a plain space, a long multi-token ayah, an ayah spanning a page boundary with a non-space separator, a muqattaʿat ayah whose disjointed letters are a standalone token, a legitimate repeated refrain across two ayahs (to exercise the QV-022 Info path), a superscript alef U+0670, and small high signs so grapheme-cluster counting matters. Do not declare an upstream identity, primary flag, or license on this fixture — that belongs to `test-edition-identity` in Task 2 — and do not name a real upstream dataset, publisher, or license anywhere in these files. Provide the companion CSV mirror `ayahs.csv` whose rows reproduce the JSON ayahs exactly, and extend `fixtures/quran/golden/ayah_texts.jsonl` with one row per rich-fixture ayah. Extend `crates/quran-corpus/tests/fixtures.rs` with a shape test that parses the rich fixture, asserts slug/counts, asserts `synthetic: true` (never canonical, mirroring the `test-edition-min` assertion), asserts no duplicate `(surah, ayah)` ids, asserts dense per-surah ayah numbering, asserts the pause-mark/basmala-in-body/muqattaʿat/refrain/U+0670 coverage markers are present, and asserts the CSV adapter reproduces the JSON ayahs field-for-field; add a golden-text test for the rich fixture. Keep `test-edition-min` and all 16 adversarial corpora byte-identical.
  </action>
  <verify>
    <automated>cargo test -p quran-corpus --test fixtures -- --nocapture</automated>
    <fails_when>non-zero exit, or "0 passed", or a parse/shape/CSV-parity/golden/coverage-marker assertion failing for the rich fixture</fails_when>
    <automated>cargo test -p quran-corpus --test adversarial</automated>
    <fails_when>non-zero exit, or "0 passed", or any of the 16 adversarial fixtures no longer being rejected by its asserted rule id</fails_when>
    <automated>git status --short fixtures/quran/test-edition-min fixtures/quran/adversarial</automated>
    <fails_when>any line in the output, which would mean an existing fixture was modified instead of extended</fails_when>
  </verify>
  <done>A richer synthetic edition with CSV/golden mirrors parses, round-trips through the CSV adapter, exercises the named integrity stressors, and stays explicitly synthetic; the existing fixture and 16 adversarial corpora are untouched.</done>
  <acceptance_criteria>
    - `fixtures/quran/test-edition-rich/manifest.json` declares `synthetic: true`, a distinct slug, and an `expected` block with both reference pins set.
    - The fixture exercises, at minimum: a basmala inside an ayah body, at least one U+06D6…U+06ED pause mark, a mark-separated token, a long ayah, a page boundary, a muqattaʿat token, a repeated refrain, and U+0670.
    - The CSV mirror reproduces the JSON ayahs exactly under the existing `CsvAdapter`.
    - Existing fixtures are unmodified (`git status` clean for `test-edition-min` and `adversarial`).
    - No real upstream dataset slug, publisher, or license appears in the new fixture files.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Companion synthetic reference and the identity/primary/license fixture</name>
  <files>fixtures/quran/test-edition-rich/reference.json, fixtures/quran/test-edition-identity/manifest.json, crates/quran-corpus/tests/fixtures.rs</files>
  <read_first>
    - fixtures/quran/test-edition-rich/manifest.json (created in Task 1)
    - fixtures/quran/test-edition-min/manifest.json (`edition.license`/`upstream_edition_slug`/`qai_edition_id` shape; these parse through `EditionMeta`)
    - crates/quran-corpus/src/format.rs (`EditionMeta.is_primary`, `upstream_edition_slug`, `qai_edition_id`, `license`; added/parsed in plan 02-01)
    - crates/quran-corpus/src/import.rs (`compare_reference`, the QV-015 byte-only semantics, and `ExpectedCounts.reference_corpus_id`/`reference_text_hash`)
    - crates/quran-corpus/tests/fixtures.rs (the rich-fixture tests added in Task 1)
    - docs/02-architecture/decisions/ADR-0114-reference-corpus-comparison.md (§4 byte-only pass/fail)
  </read_first>
  <action>
    Add two fixtures, both synthetic and non-canonical. First, `fixtures/quran/test-edition-rich/reference.json`: a companion synthetic reference edition conforming to the same `EditionSource` schema, `synthetic: true`, whose ayahs match the rich fixture byte-for-byte, with `expected.reference_corpus_id` set to a clearly-synthetic identifier (e.g. `synthetic-rich-reference`) and `expected.reference_text_hash` set to the matching pin value; it must NOT name a real upstream dataset, publisher, license, or signer — the real reference-corpus identity/scope/license stays owner-gated as OD-03 (recorded in plan 02-07). Do not compute the pin by any new recipe: reuse the existing manifest-hash helper and the existing hashing primitives, and if the pin value cannot be derived without inventing a recipe, record the pin as the existing `sha256:<hex>` form over the reference's ayah text stream as produced by the existing hashing module. Second, `fixtures/quran/test-edition-identity/manifest.json`: a small `EditionSource` (`synthetic: true`, a distinct slug, a handful of ayahs) that declares a non-null, clearly-synthetic `upstream_edition_slug` and `qai_edition_id`, the primary/default flag (`is_primary: true`), and a concrete declared license (`license.status: "verified"` plus an SPDX expression string) — this is the fixture plan 02-03's `edition_identity` CLI snapshot and its application-level identity assertions import. It must not name a real dataset or license. Extend `crates/quran-corpus/tests/fixtures.rs` with: a reference-shape test asserting `reference.json` parses as a valid `EditionSource`, is `synthetic: true`, carries both reference pins, and names no real upstream identity; and an identity-fixture test asserting `test-edition-identity/manifest.json` parses with a non-null `upstream_edition_slug`, a non-null `qai_edition_id`, `is_primary == true`, and a non-`unknown` declared license status. Do not modify `test-edition-min` or the adversarial corpora.
  </action>
  <verify>
    <automated>cargo test -p quran-corpus --test fixtures -- --nocapture</automated>
    <fails_when>non-zero exit, or "0 passed", or a reference-shape or identity-fixture assertion failing</fails_when>
    <automated>git status --short fixtures/quran/test-edition-min fixtures/quran/adversarial</automated>
    <fails_when>any line in the output, which would mean an existing fixture was modified instead of extended</fails_when>
  </verify>
  <done>A companion synthetic reference with declared pins and a synthetic identity/primary/license fixture both parse as valid `EditionSource` values and are provably non-canonical; no real identity or license is introduced.</done>
  <acceptance_criteria>
    - `test-edition-rich/reference.json` is a schema-valid `EditionSource` marked `synthetic: true` with both `expected` reference pins set.
    - `test-edition-identity/manifest.json` declares `is_primary: true`, non-null `upstream_edition_slug`/`qai_edition_id`, and a concrete declared license, all `synthetic: true`.
    - Tests assert each fixture's parsed shape; no real upstream slug, publisher, or license appears in either file.
    - No existing fixture, migration, or source file is modified by this task.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
**Posture:** ASVS Level 1; any unmitigated `high`-severity threat blocks this plan — no auto-approval.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture bytes → adapters/tests | Synthetic fixture JSON/CSV is parsed by the same adapters used for operator input; a malformed or identity-bearing fixture would mislead later surfaces. |
| fixture identity fields → operator surfaces | A fixture that names a real dataset, license, or signer would fabricate provenance on every surface that echoes it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-32 | Repudiation | rich/identity/reference fixture identity fields | high | mitigate | Every fixture is `synthetic: true`; tests assert synthetic-ness and that no real dataset, publisher, license, or signer is named; OD-01/OD-03 remain recorded owner gates. |
| T-02-33 | Tampering | existing fixtures overwritten instead of extended | medium | mitigate | Keep `test-edition-min` and the 16 adversarial corpora byte-identical; a `git status --short` gate fails on any modification. |
| T-02-34 | Spoofing | reference fixture presented as the independent (text-bearing) corpus | medium | mitigate | The reference is explicitly synthetic and pinned by the existing hashing form; the real corpus identity remains owner-gated (OD-03). |
| T-02-35 | Information disclosure | UV/encoding stressors in fixture text | low | accept | Fixture text is synthetic Quranic-shape data, non-secret by design. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this phase (RESEARCH.md § Package Legitimacy Audit). Any future install requires the package-legitimacy gate plus a blocking human checkpoint. |
</threat_model>

<verification>
- `cargo test -p quran-corpus --test fixtures` — rich, reference, and identity fixtures parse; CSV parity and golden text hold.
- `cargo test -p quran-corpus --test adversarial` — all 16 adversarial corpora still rejected by their rule ids.
- `git status --short fixtures/quran/test-edition-min fixtures/quran/adversarial` — clean, proving extension not modification.
</verification>

<success_criteria>
- The exercised dataset is materially deeper (D-08) and includes a valid identity/primary/license fixture for the operator path.
- Every new fixture is synthetic, non-canonical, and free of invented real-world identity.
- All existing fixtures remain byte-identical.
</success_criteria>

## Artifacts this plan produces

| Artifact | Kind | Consumer |
|---|---|---|
| `fixtures/quran/test-edition-rich/{manifest.json,ayahs.csv}` | fixtures | importer tests, plan 02-03 snapshots, plan 02-07 artifact |
| `fixtures/quran/test-edition-rich/reference.json` | fixture | plan 02-03 reference path, plan 02-07 artifact |
| `fixtures/quran/test-edition-identity/manifest.json` | fixture | plan 02-03 identity/primary snapshot |
| `fixtures/quran/golden/ayah_texts.jsonl` (extended) | golden data | golden-text test |
| `crates/quran-corpus/tests/fixtures.rs` (extended) | test suite | `cargo test -p quran-corpus` |

<output>
Create `.planning/phases/02-canonical-quran-core/02-02-SUMMARY.md` when done
</output>
