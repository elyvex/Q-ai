---
phase: 02-canonical-quran-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/0020_quran_edition_identity.up.sql
  - migrations/sqlite/checksums.json
  - crates/quran-corpus/src/format.rs
  - docs/schemas/quran-edition-source.v1.schema.json
  - crates/quran-corpus/src/import.rs
  - crates/storage/src/quran.rs
  - crates/storage-sqlite/src/quran.rs
  - crates/quran-core/src/edition.rs
  - crates/quran-core/src/enums.rs
  - crates/application/src/quran_reader.rs
  - crates/application/src/quran_cli.rs
  - crates/application/tests/quran_identity.rs
autonomous: true
requirements:
  - REQ-quran-corpus
  - REQ-data-separation-layers
user_setup: []
estimate:
  tokens: 58000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`EditionMeta` (crates/quran-corpus/src/format.rs) gains `is_primary: bool` (serde-defaulted) and `docs/schemas/quran-edition-source.v1.schema.json` declares the matching boolean property (additionalProperties:false stays intact), so the manifest can declare the primary/default designation and it can be sourced; no value is persisted that is not declared on an existing type. The count/numbering column semantics are unchanged (D-07)."
    - "An edition imported from a manifest that declares upstream_edition_slug, qai_edition_id, and is_primary exposes all three verbatim on the canonical row and through the operator `edition show` surface (application `cmd_edition_show`, asserted in `quran_identity.rs`; the CLI trycmd snapshot lands in plan 02-03); a manifest that declares none leaves NULL/None/false and no value is invented (QC-09, D-07; edge probe 'empty input': absent input stays absent)."
    - "The manifest-declared license is persisted verbatim on the canonical edition and surfaced as the doctor license status; the operator import path no longer synthesizes a placeholder license status (QC-09, D-06)."
    - "`EditionSelector::Primary` resolves to the edition explicitly flagged primary and never silently falls back to the active pointer; 'primary default' is not 'only edition' (D-07). The lookup reads canonical editions where `is_primary` is set and returns a typed error when none is flagged (or when more than one is), so a caller cannot believe it read 'the default' when none is declared."
    - "Migration 0020 is append-only and additive: migrations 0007–0019 are byte-identical, checksums.json gains exactly one new entry, and `xtask migrate-check` reports the ordered migration set including 0020 (edge probe 'concurrency/interruption': a migration interrupted before commit leaves the applied set unchanged)."
    - "Edition identity equality (slug, version, upstream_edition_slug) is byte-exact over the stored form; no comparison normalizes, case-folds, or grapheme-collapses an identity value (edge probe 'encoding')."
  artifacts:
    - "migrations/sqlite/0020_quran_edition_identity.up.sql"
    - "crates/application/tests/quran_identity.rs (new integration suite)"
    - "EditionMeta::is_primary (crates/quran-corpus/src/format.rs) + the `is_primary` boolean property in docs/schemas/quran-edition-source.v1.schema.json"
    - "columns upstream_edition_slug, qai_edition_id, is_primary on quran_editions and quran_stg_editions"
    - "fields upstream_edition_slug, qai_edition_id, is_primary on storage::quran::QuranEditionRow and quran_core::QuranEdition"
    - "EditionSelector::Primary variant"
  key_links:
    - "EditionMeta (crates/quran-corpus/src/format.rs: parses upstream_edition_slug/qai_edition_id/license, gains is_primary) → quran_stg_editions columns → quran_editions via the INSERT..SELECT inside SqliteQuranRepository::activate_edition → QuranEditionRow → QuranEdition → `qai quran edition show`"
    - "docs/schemas/quran-edition-source.v1.schema.json `edition.is_primary` (type boolean) → JsonAdapter parse of a manifest declaring the flag → EditionMeta.is_primary → staging/activation"
    - "EditionSelector::Primary → QuranReaderService::resolve_edition → QuranRepository::list_editions filtered on is_primary → the same byte-exact build_view path (must not introduce a second construction path that omits the stored text_hash)"
    - "operator import path (crates/application/src/quran_cli.rs cmd_import) → ImportInput.license_status/license_json → quran_stg_editions/license_json → canonical license_json"
  prohibitions:
    - "No agent, model, or importer may generate, correct, or substitute canonical text; only verbatim manifest bytes are stored. [flagged-unverified]"
    - "No canonical row may be written without an ApprovalToken bound to the exact quran-edition:{slug}@{version} URN. [flagged-unverified]"
    - "The importer must have no code path to canonical tables; it stages only. [flagged-unverified]"
    - "The importer must never invent a dataset slug, publisher, release, license, reviewer, or reference-corpus identity; absent manifest values stay absent and are recorded as owner gates OD-01/OD-02/OD-03. [flagged-unverified]"
    - "riwayah/qiraah/script must never be inferred from a publisher, filename, or script shape; only explicit manifest values are stored. [flagged-unverified]"
    - "A translation or normalization text must never be substituted into a canonical slot. [flagged-unverified]"
    - "Never present a QV-015 reference-comparison skip as a pass. [flagged-unverified]"
---

<objective>
Lead the phase with the thinnest true vertical slice that crosses every layer Phase 2 will touch: one migration, one pipeline field, one storage path, one domain type, one operator surface. Persist and surface the edition's declared upstream identity and default designation so QC-09 (upstream identity dropped after parsing), QC-11 (no primary/default flag), and Pitfall 6 (canonical row does not carry the upstream identity) close on a proven seam.

Purpose: Phase 2 is evidence-driven brownfield hardening (D-01). The highest-risk assumption in the research log is A3 — that an append-only migration can add columns to `quran_editions`/`quran_stg_editions` and flow them through the frozen staging→activation move without editing 0007–0019 or rebuilding the transaction. This plan proves that assumption end-to-end before any expansion task spends context on fences, quotation, or translation work.

Task ledger: `TASK-002-canonical-quran-core` (see `docs/04-tasks/`) — this plan is one implementation unit of that phase task.

Output: migration `0020`, new columns plumbed from the manifest (including the new schema-declared `is_primary`) to the operator surface, `EditionSelector::Primary`, and executable evidence (`cargo test -p application --test quran_identity`, `cargo run -q -p xtask -- migrate-check`; the `qai quran edition show` CLI trycmd snapshot lands in plan 02-03, while this plan asserts the same surface at the application level).
</objective>

<execution_context>
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/workflows/execute-plan.md
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-canonical-quran-core/02-CONTEXT.md
@.planning/phases/02-canonical-quran-core/02-RESEARCH.md
@.planning/phases/02-canonical-quran-core/02-PATTERNS.md
@migrations/sqlite/0007_quran_editions.up.sql
@migrations/sqlite/0011_quran_staging.up.sql
@crates/quran-corpus/src/format.rs
@docs/schemas/quran-edition-source.v1.schema.json
@crates/storage-sqlite/src/quran.rs
@crates/quran-core/src/edition.rs
@crates/quran-core/src/enums.rs
@crates/application/src/quran_reader.rs
@crates/application/src/quran_cli.rs
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end edition identity — migration 0020 through the operator surface</name>
  <files>migrations/sqlite/0020_quran_edition_identity.up.sql, migrations/sqlite/checksums.json, crates/quran-corpus/src/format.rs, docs/schemas/quran-edition-source.v1.schema.json, crates/quran-corpus/src/import.rs, crates/storage/src/quran.rs, crates/storage-sqlite/src/quran.rs, crates/quran-core/src/edition.rs, crates/application/tests/quran_identity.rs</files>
  <read_first>
    - migrations/sqlite/0007_quran_editions.up.sql (current `quran_editions` columns and the existing identity/hash trigger)
    - migrations/sqlite/0011_quran_staging.up.sql (the `quran_stg_editions` mirror shape)
    - crates/quran-corpus/src/format.rs (`EditionMeta` already parses `upstream_edition_slug`, `qai_edition_id`, `license`, `verified_by`, `source`; this task adds `is_primary` and updates the `sample()` literal)
    - docs/schemas/quran-edition-source.v1.schema.json (`edition` object with `additionalProperties:false`; the `upstream_edition_slug`/`qai_edition_id`/`license` properties are already declared — this task adds `is_primary`)
    - crates/quran-corpus/src/import.rs (`staged()` and the staging insert call site)
    - crates/storage-sqlite/src/quran.rs (`insert_stg_edition` explicit column list; `activate_edition` staging→canonical move; `decode_edition`)
    - crates/storage/src/quran.rs (`QuranEditionRow` and the gated-mutator contract)
    - crates/quran-core/src/edition.rs (`QuranEdition`, `is_readable()`)
    - crates/application/tests/quran_import.rs (harness helpers: `migrated_db`, principal, timestamp, staged-run setup)
    - .planning/codebase/CONVENTIONS.md (naming, typed errors, migration rules)
  </read_first>
  <action>
    Wire ONE field family through every layer, nothing else. First source the primary/default designation from the manifest, because it does not exist as a type today: add `#[serde(default)] pub is_primary: bool` to `EditionMeta` in `crates/quran-corpus/src/format.rs` (and set it to `false` in the `sample()` literal), and add `"is_primary": { "type": "boolean" }` to the `edition` object's `properties` in `docs/schemas/quran-edition-source.v1.schema.json` — the schema sets `additionalProperties:false`, so the property must be declared for a fixture manifest to carry it; `gen-schema` regenerates only `docs/schemas/index.json`, so the committed schema edit is CI-quiet under `gen-schema && git diff --exit-code docs/schemas/`. Then add a new append-only migration file `migrations/sqlite/0020_quran_edition_identity.up.sql` (latest applied is 0019; never edit 0007–0019) that ALTER TABLE ADD COLUMN on BOTH `quran_editions` and `quran_stg_editions` for `upstream_edition_slug TEXT`, `qai_edition_id TEXT`, and `is_primary INTEGER NOT NULL DEFAULT 0`. There is no in-tree ALTER TABLE precedent, so use plain `ALTER TABLE ... ADD COLUMN` (SQLite supports it) and keep the file comment-free of any claim that these columns are already trigger-guarded — the immutability trigger for them lands in plan 02-04's migration. Update `migrations/sqlite/checksums.json` with the new file's checksum only. Carry the parsed `EditionMeta` values into the `quran_stg_editions` insert: extend the staging-insert column list and its bound values in `crates/storage-sqlite/src/quran.rs`, and pass `source.edition.upstream_edition_slug` / `source.edition.qai_edition_id` / `source.edition.is_primary` from the importer's staging call in `crates/quran-corpus/src/import.rs`. Add the three columns to the explicit column list of the `INSERT INTO quran_editions ... SELECT ... FROM quran_stg_editions` statement inside `SqliteQuranRepository::activate_edition` so activation preserves them; add the three fields to `storage::quran::QuranEditionRow`, its `decode_edition` mapping, and `quran_core::QuranEdition`. Defensively default absent manifest values to NULL/None/false rather than any invented string. Do NOT add a row-level canonical mutator, do NOT change the activation transaction's ordering, pointer flip, or generation bump, and do NOT touch `qai-text-hash-v1` or any frozen hash recipe. Add a new integration suite `crates/application/tests/quran_identity.rs` that imports a manifest declaring a non-null `upstream_edition_slug` plus `qai_edition_id` plus `is_primary: true`, activates it, and asserts the canonical edition row, the reader's edition view, and `application::quran_cli::cmd_edition_show` (human + `--json`) all carry the declared values and the primary marker verbatim; build that manifest by `include_str!`-ing `fixtures/quran/test-edition-min/manifest.json`, parsing it into `EditionSource`, setting the three fields, and re-serializing — do not add a new fixture file in this plan (plan 02-02 owns fixtures). Add a second case asserting an undeclared manifest yields NULL/None/false, and a third asserting `EditionSelector::Primary` resolves to the flagged edition while an unflagged store returns a typed error.
  </action>
  <verify>
    <automated>cargo run -q -p xtask -- migrate-check</automated>
    <fails_when>non-zero exit, or output not containing "OK", or a checksum/ordering error naming 0020 or any of 0007–0019</fails_when>
    <automated>cargo test -p application --test quran_identity -- --nocapture</automated>
    <fails_when>non-zero exit, or a test result line reporting 0 passed, or any assertion that the declared upstream_edition_slug/qai_edition_id/is_primary is absent after activation</fails_when>
    <automated>cargo test -p storage-sqlite --test quran</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or the activation/rollback row-move tests failing</fails_when>
  </verify>
  <done>A manifest-declared `upstream_edition_slug`, `qai_edition_id`, and `is_primary` survive import → staging → activation and are readable through `QuranEdition`; `EditionMeta.is_primary` and the schema property that source them exist; an undeclared manifest stores NULL/None/false; migration 0020 is the only migration change and `migrate-check` is green.</done>
  <acceptance_criteria>
    - `git status` shows no modification to `migrations/sqlite/0007_*.up.sql` … `0019_*.up.sql`; the only new/changed migration artifacts are `0020_quran_edition_identity.up.sql` and `checksums.json`.
    - `EditionMeta` declares `is_primary` (serde-defaulted) and `docs/schemas/quran-edition-source.v1.schema.json` declares the matching `edition.is_primary` boolean property with `additionalProperties:false` preserved; `cargo run -q -p xtask -- gen-schema` leaves `docs/schemas/` unchanged.
    - `quran_editions` and `quran_stg_editions` both have `upstream_edition_slug`, `qai_edition_id`, `is_primary` after migration 0020.
    - The canonical row produced by activation carries the manifest-declared `upstream_edition_slug` byte-for-byte (no trim, no case fold, no normalization of the identity string) and the declared `is_primary` value.
    - No new public canonical mutator appears on the `QuranRepository` trait (the existing source-scan test in `crates/storage/src/quran.rs` still passes unchanged).
    - `cargo run -q -p xtask -- arch-check` still reports no forbidden dependency edges.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Persist the manifest-declared license instead of a synthesized Unknown</name>
  <files>crates/application/src/quran_cli.rs, crates/quran-corpus/src/import.rs, crates/application/tests/quran_identity.rs</files>
  <read_first>
    - crates/application/src/quran_cli.rs (`cmd_import`: the hardcoded `license_status: "Unknown"` and synthetic license JSON)
    - crates/quran-corpus/src/format.rs (`EditionMeta.license`, `EditionMeta.upstream_edition_slug`)
    - crates/quran-corpus/src/import.rs (the license_status/license_json fields on `ImportInput` and how they reach staging)
    - migrations/sqlite/0007_quran_editions.up.sql (`license_json` column)
    - crates/application/src/quran_doctor.rs (`quran.license_status` check reads the `status` key of `license_json`)
    - crates/application/tests/quran_identity.rs (suite created in Task 1)
  </read_first>
  <action>
    Stop synthesizing license identity in the operator import path. In `crates/application/src/quran_cli.rs::cmd_import`, derive `license_status` and `license_json` from the manifest's declared license via `EditionMeta` (already parsed in `crates/quran-corpus/src/format.rs`) instead of the constant `"Unknown"` plus a fabricated `redistribution_allowed: false` record. When the manifest declares no license, keep an explicit `Unknown` status and record it as an owner gate (OD-01) — do not rename an absent license to a permissive or restrictive one. Preserve `trust_level`/`verification_status` semantics unchanged. Preserve the value verbatim (no trimming of the declared license identifier, no license inference from the publisher or repository). Because the declared license must reach the canonical row, verify the `license_json` written by activation equals the declared manifest license JSON; if the staging path currently drops it, carry it through the same explicit column-list style used in Task 1 — do not introduce a second publication path. Extend `crates/application/tests/quran_identity.rs` with a case importing a manifest that declares a concrete license and asserting the canonical `license_json` status equals the declared value verbatim, plus a case with no declared license asserting `Unknown` and no invented redistribution flags.
  </action>
  <verify>
    <automated>cargo test -p application --test quran_identity -- --nocapture</automated>
    <fails_when>non-zero exit, or 0 passed, or the declared-license case asserting anything other than the verbatim declared status</fails_when>
    <automated>cargo test -p application --test quran_import -- --nocapture</automated>
    <fails_when>non-zero exit, or 0 passed, or any pre-existing import/activation case regressing because the license payload shape changed</fails_when>
  </verify>
  <done>The canonical edition's `license_json` reflects the manifest-declared license verbatim; an undeclared license stays explicitly `Unknown` with no invented redistribution flags; owner gate OD-01 is the only remaining blocker for a real license.</done>
  <acceptance_criteria>
    - No string literal placeholder license is written by `cmd_import` when the manifest declares a license.
    - A test asserts the canonical `license_json` status equals the manifest-declared status character-for-character.
    - A test asserts an undeclared license yields status `Unknown` and does not set `redistribution_allowed` to true.
    - OD-01 is not "closed" by this task: no dataset slug, publisher, release, or license value is invented anywhere in the diff.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Explicit primary/default designation feeding the reader selector</name>
  <files>crates/quran-core/src/enums.rs, crates/quran-core/src/edition.rs, crates/application/src/quran_reader.rs, crates/application/src/quran_cli.rs, crates/application/tests/quran_identity.rs</files>
  <read_first>
    - crates/quran-core/src/enums.rs (`EditionSelector`: `Active` / `Slug` / `Pinned`)
    - crates/quran-core/src/edition.rs (`QuranEdition` fields and `is_readable()` derived-method style)
    - crates/application/src/quran_reader.rs (`resolve_edition` selector arms and the byte-exact `build_view` that must keep serving the stored `text_hash`)
    - crates/application/src/quran_cli.rs (`cmd_edition_show` human line; `parse_selector` for `--edition`-style arguments)
    - crates/application/tests/quran_import.rs (approval-row harness and activation helpers reused by the primary-resolution case)
    - crates/storage/src/quran.rs (`list_editions` read-only signature; the gated-mutator source-scan test that must stay green)
  </read_first>
  <action>
    Make the primary/default designation explicit and queryable without redefining the activation pointer. Add `EditionSelector::Primary` (serde `snake_case`, matching the existing variants) and a `resolve_edition` arm in `crates/application/src/quran_reader.rs` that resolves to the edition whose `is_primary` flag is set. Implement the lookup by reusing the existing read-only `QuranRepository::list_editions()` on the same `UnitOfWork` the arm already opens and filtering rows on `is_primary` — do not add a new repository method and do not add a canonical mutator (the gated-mutator source-scan test must stay green). Return a typed `ReaderError` when zero editions are flagged (never silently fall back to the `quran_active_edition` pointer) and a distinct typed error when more than one row is flagged, so the caller cannot believe it read "the default" when none is uniquely declared; record in the arm's doc comment that `Active` keeps its existing meaning and that "primary default" is not "only edition" (D-07). Keep the resolution on the existing `build_view` path so the stored per-ayah `text_hash` is always attached — do not add a second construction path. Surface `upstream_edition_slug`, `qai_edition_id`, and the primary flag on the `qai quran edition show` human line in `crates/application/src/quran_cli.rs` and confirm they already appear in `--json` through `QuranEdition`; keep the human line free of volatile ids so a snapshot is deterministic. Assert this surface at the application level in `crates/application/tests/quran_identity.rs` by calling `application::quran_cli::cmd_edition_show` (human and `--json`) after activating the identity manifest from Task 1 and asserting the declared identity and primary values; the CLI trycmd snapshot that exercises the same surface through the `qai` binary (`crates/cli/tests/quran/edition_identity.trycmd`) lands in plan 02-03 Task 3. Do not add a runtime setter for `is_primary` in this phase: the flag is declared at import and carried by activation; a re-designation path would be a new approval-gated canonical mutator and is out of scope.
  </action>
  <verify>
    <automated>cargo test -p application --test quran_identity -- --nocapture</automated>
    <fails_when>non-zero exit, or 0 passed, or a case where primary resolution returns the active edition instead of a typed absent-primary error</fails_when>
    <automated>cargo test -p application --test quran_reader</automated>
    <fails_when>non-zero exit, or "0 passed", or a cache/byte-exactness case failing after the selector arm was added</fails_when>
    <automated>cargo run -q -p xtask -- arch-check</automated>
    <fails_when>non-zero exit, or output not containing "OK", or a forbidden dependency edge</fails_when>
  </verify>
  <done>An edition whose manifest declares the primary/default designation resolves through `EditionSelector::Primary`; the flag and the upstream identity appear on the application-level `edition show` surface (CLI snapshot in plan 02-03); `Active`, `Slug`, and `Pinned` semantics and the byte-exact reader path are unchanged.</done>
  <acceptance_criteria>
    - `EditionSelector::Primary` exists, is serde-compatible with the existing variants, and has a reader arm.
    - The reader arm errors (typed, non-panicking) when no edition carries the primary flag and when more than one does; it never silently returns the active edition.
    - No new repository method and no canonical mutator is added for the primary lookup (the gated-mutator source-scan test stays green).
    - `cmd_edition_show` (human and `--json`) reports the declared upstream slug and primary marker; the CLI trycmd snapshot for the same surface is owned by plan 02-03.
    - All `quran_reader` byte-exactness and cache-invalidation tests remain green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
**Posture:** ASVS Level 1; any unmitigated `high`-severity threat blocks this plan — no auto-approval.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest bytes → importer | Untrusted operator-supplied JSON crosses here; every value is parsed into a typed intermediate format before any storage write. |
| staging (`quran_stg_*`) → canonical (`quran_*`) | The only canonical-write boundary; crossed solely inside one approval-gated `UnitOfWork` in `activate_edition`. |
| canonical row → operator CLI output | Canonical identity/license values are rendered to the operator; a fabricated value here would misrepresent provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | migration 0020 (`ALTER TABLE quran_editions` / `quran_stg_editions`) | medium | mitigate | Append-only new file; never edit 0007–0019; `checksums.json` gains one entry; gate with `cargo run -q -p xtask -- migrate-check`. Additive nullable/defaulted columns cannot rewrite existing canonical bytes. |
| T-02-02 | Repudiation | `cmd_import` license synthesis | high | mitigate | Persist the manifest-declared license verbatim; absent license stays explicit `Unknown` and is recorded as OD-01. Never infer a license from a repository or publisher. |
| T-02-03 | Spoofing | `EditionSelector::Primary` resolution | medium | mitigate | Resolution never falls back to the active pointer; an unset primary flag returns a typed error so a caller cannot believe it read "the default" when none is declared. |
| T-02-04 | Tampering | new columns reachable by a raw `UPDATE` before the fence migration lands | medium | accept (transient, with escalation) | Immutability triggers for these columns land in plan 02-04's migration 0021; until then only the activation `INSERT..SELECT` writes them and no repository mutator exists. Plan 02-04 makes the window unobservable by adding the `BEFORE UPDATE OF` trigger and its adversarial test. |
| T-02-05 | Information disclosure | edition identity/license surfaced in CLI output | low | accept | Values are non-secret provenance metadata; no secret or credential crosses this boundary. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this phase (RESEARCH.md § Package Legitimacy Audit: no new package proposed). Any future install requires the package-legitimacy gate and a blocking human checkpoint; no `[ASSUMED]`/`[SUS]` package is approved here. |
</threat_model>

<verification>
- `cargo run -q -p xtask -- migrate-check` — 20 ordered migrations, checksums stable, 0007–0019 untouched.
- `cargo test -p application --test quran_identity` — declared identity/license/primary survive import → activation → the application-level `edition show` surface; undeclared values stay absent.
- `cargo test -p storage-sqlite --test quran` — the frozen staging→canonical move, pointer flip, and generation bump still pass.
- `cargo test -p quran-corpus --test fixtures` — the added `EditionMeta.is_primary` field parses the existing fixtures unchanged (no regression from the struct change).
- `cargo run -q -p xtask -- gen-schema && git diff --exit-code docs/schemas/` — the committed schema edit is CI-quiet.
- `cargo run -q -p xtask -- arch-check` — no forbidden dependency edges.
</verification>

<success_criteria>
- One validated edition can be imported such that its declared upstream identity, declared license, and default designation are all retrievable from the canonical edition and the operator CLI.
- The append-only migration assumption (research A3) is proven end-to-end before any expansion task.
- No canonical text, hash recipe, reference grammar, or activation transaction behavior changed.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Consumer |
|---|---|---|
| `migrations/sqlite/0020_quran_edition_identity.up.sql` | migration | storage-sqlite migration runner |
| `migrations/sqlite/checksums.json` | checksum ledger | `xtask migrate-check` |
| `quran_editions.upstream_edition_slug`, `.qai_edition_id`, `.is_primary` | columns | storage / reader / CLI |
| `quran_stg_editions.upstream_edition_slug`, `.qai_edition_id`, `.is_primary` | columns | importer staging |
| `storage::quran::QuranEditionRow::{upstream_edition_slug, qai_edition_id, is_primary}` | fields | storage-sqlite decode/encode |
| `quran_core::QuranEdition::{upstream_edition_slug, qai_edition_id, is_primary}` | fields | reader, CLI |
| `quran_core::EditionSelector::Primary` | enum variant | reader `resolve_edition` |
| `quran_corpus::EditionMeta::is_primary` + `docs/schemas/quran-edition-source.v1.schema.json` (`edition.is_primary`) | field + schema property | manifest parsing / importer |
| `crates/application/tests/quran_identity.rs` | test suite | `cargo test -p application` |
| `crates/cli/tests/quran/edition_identity.trycmd` + harness fn in `crates/cli/tests/quran.rs` | CLI snapshot (produced by plan 02-03) | `cargo test -p cli --test quran` |

<output>
Create `.planning/phases/02-canonical-quran-core/02-01-SUMMARY.md` when done
</output>
