---
phase: 02-canonical-quran-core
plan: 07
type: execute
wave: 5
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
  - 02-06
files_modified:
  - docs/06-progress/corpus-integrity-report.json
  - docs/06-progress/phase-02-evidence.md
  - docs/06-progress/task-done-rollup.md
  - docs/05-followups/phase-02-owner-gates.md
  - docs/05-followups/decisions-needed.md
  - docs/04-tasks/active/TASK-002-canonical-quran-core.md
  - docs/04-tasks/active/README.md
  - docs/04-tasks/completed/TASK-002-canonical-quran-core.md
  - docs/04-tasks/completed/README.md
  - crates/cli/tests/corpus_integrity.rs
  - xtask/src/coverage.rs
files_deleted:
  - docs/04-tasks/active/TASK-002-canonical-quran-core.md
autonomous: true
requirements:
  - REQ-quran-corpus
  - REQ-data-separation-layers
  - REQ-ingestion-validation-eval
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Owner gates OD-01 (dataset + license), OD-02 (named reviewer + `verified_by`), and OD-03 (independent reference corpus) are recorded as explicit blocked/deferred items with the exact action and command that closes each, and none is presented as satisfied by this phase (D-03, D-06, D-09; edge probes 'empty'/'encoding' are answered by the recorded evidence, not by an invented value)."
    - "A committed corpus-integrity report artifact exists as the phase's evidence of record, classifies all six integrity families, and marks the reference family honestly (`skipped` when no reference corpus was configured) — a skipped family is never recorded as passed (D-10, D-11)."
    - "Each of the five roadmap success criteria is mapped to a named command whose actual output is recorded, and any red result is attributed to its real owner rather than absorbed (including the known legacy `search.trycmd`/`normalize.trycmd` baseline, which belongs to the search/normalization phase)."
    - "The published legacy coverage floors for the Quran crates are either implemented in the gate or explicitly recorded as unmet with the measured gap; they are never left silently unmet (QC-12)."
    - "Every unresolved row from the spec-less edge-probe fallback is enumerated and dispositioned: the six rows (REQ-quran-corpus: `empty`, `encoding`; REQ-data-separation-layers: `unclassified`; REQ-ingestion-validation-eval: `empty`, `encoding`, `concurrency`) each name the plan and acceptance criterion that answers them, and none is silently dropped."
    - "The D-15 read-path exemption (direct-read CLI/HTTP/tool paths that serve the canonical source of truth and cannot mismatch by construction) is recorded as an owner-ratifiable interpretation of the locked decision, never as a silently closed scope reduction (D-15)."
    - "AGENTS.md's task-ledger contract is honoured: `TASK-002-canonical-quran-core` records the phase task, maps plans 02-01…02-07 to the five roadmap success criteria, and moves active → completed with dated evidence and a rollup entry — never marked complete before its acceptance criteria are satisfied."
  artifacts:
    - "docs/06-progress/corpus-integrity-report.json (committed evidence of record)"
    - "docs/06-progress/phase-02-evidence.md (criterion → command → observed result, plus the edge-probe disposition and read-path exemption)"
    - "docs/05-followups/phase-02-owner-gates.md (OD-01/OD-02/OD-03 blocked-gate records with closing actions, plus the owner-ratifiable D-15 exemption and any coverage shortfall)"
    - "docs/04-tasks/completed/TASK-002-canonical-quran-core.md (closed phase task record) + the active→completed index updates + rollup entry"
    - "crates/cli/tests/corpus_integrity.rs (artifact reproducibility/classification test)"
    - "xtask/src/coverage.rs THRESHOLDS reconciled for quran-core / quran-corpus / citations (or a recorded shortfall follow-up)"
  key_links:
    - "`qai quran verify --json` on the rich fixture → docs/06-progress/corpus-integrity-report.json → crates/cli/tests/corpus_integrity.rs assertions"
    - "five success criteria → exact commands → recorded observed output in docs/06-progress/phase-02-evidence.md"
    - "OD-01 → ADR-0101 acceptance; OD-02 → `qai quran edition verify` stamp; OD-03 → ADR-0114 selection + `qai quran import --reference`"
    - "legacy coverage floors (docs/03-plan/phases/phase-01-core/acceptance.md §4) → xtask/src/coverage.rs THRESHOLDS or a recorded shortfall"
    - "spec-less edge-probe rows → docs/06-progress/phase-02-evidence.md disposition table → the answering plan's acceptance criteria"
    - "TASK-002-canonical-quran-core → plans 02-01…02-07 + the five roadmap success criteria → docs/06-progress/task-done-rollup.md"
  prohibitions:
    - "An owner gate must never be closed by an agent; no dataset slug, publisher, release, license, reviewer name, reference-corpus identity, or signer may be invented or inferred. [flagged-unverified]"
    - "Never present a QV-015 reference-comparison skip as a pass in any report or evidence artifact. [flagged-unverified]"
    - "No agent, model, or importer may generate, correct, or substitute canonical text. [flagged-unverified]"
    - "Canonical coverage thresholds must never be lowered silently; any threshold set below a published floor must carry a recorded rationale and a named follow-up. [flagged-unverified]"
    - "A failing check must never be re-attributed to another phase without re-running it on a quiet worktree and recording the actual output. [flagged-unverified]"
    - "An edge-probe row must never be silently dropped; every unresolved row is either answered by an acceptance criterion or surfaced as a flagged assumption. [flagged-unverified]"
---

<objective>
Close the phase on recorded evidence rather than on narrative. Phase 2's own posture is that implementation landed but closure was never formally recorded (legacy ledger 0 of 21 verified), so the honest completion artifact is a criterion → command → observed-output record, a committed corpus-integrity artifact, an explicit blocked-gate ledger for the three human decisions, a reconciled coverage gate (QC-12), a disposition for every spec-less edge-probe row, and the repo's `TASK-002` task record closed through the AGENTS.md lifecycle.

Task ledger: `TASK-002-canonical-quran-core` (see `docs/04-tasks/`) — this plan is the closure unit of that phase task and owns its active→completed move.

Purpose: without this plan the phase could be declared complete while QC-12 stays silently unmet, OD-01/OD-02/OD-03 quietly block real activation, an edge-probe row is dropped, or the D-15 read-path interpretation is closed without the owner's knowledge. The record makes the boundary between "engineered" (this phase) and "owner-gated" (OD-01/OD-02/OD-03, D-15 exemption) unambiguous.
Output: committed evidence of record, committed integrity report, blocked-gate ledger with closing commands, the owner-ratifiable D-15 item, a reconciled coverage gate, the edge-probe disposition table, and the closed `TASK-002` task record.
</objective>

<execution_context>
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/workflows/execute-plan.md
@/Users/ali/Library/Application Support/orca/opencode-hooks/shared/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-canonical-quran-core/02-CONTEXT.md
@.planning/phases/02-canonical-quran-core/02-RESEARCH.md
@.planning/phases/02-canonical-quran-core/02-VALIDATION.md
@docs/05-followups/decisions-needed.md
@docs/03-plan/phases/phase-01-core/acceptance.md
@docs/04-tasks/README.md
@docs/04-tasks/completed/README.md
@docs/06-progress/task-done-rollup.md
@xtask/src/coverage.rs
@xtask/src/ci.rs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the three owner gates as blocked, plus the owner-ratifiable D-15 exemption</name>
  <files>docs/05-followups/phase-02-owner-gates.md, docs/05-followups/decisions-needed.md</files>
  <read_first>
    - docs/05-followups/decisions-needed.md (OD-01, OD-02, OD-03 rows, their 🔴 status and legacy ids)
    - docs/02-architecture/decisions/ADR-0101-initial-quran-dataset.md (Draft; dataset identity, licensing, bundle policy, reviewer sign-off)
    - docs/02-architecture/decisions/ADR-0114-reference-corpus-comparison.md (Draft; reference identity, procedure, signer)
    - .planning/phases/02-canonical-quran-core/02-RESEARCH.md § F-10 (what the plan must record) and § F-4 (the hash-only nature of the `spqrxi/quranchecksum` candidate) and § F-6/Pitfall 4 (the structurally exempt direct-read answer paths)
    - docs/02-architecture/decisions/ADR-0111-citation-identity-and-deep-links.md (the citation-integrity contract D-15 enforces)
    - crates/cli/src/quran.rs (`EditionAction::Verify { edition, reviewer, method }` for the OD-02 stamping command)
  </read_first>
  <action>
    Create `docs/05-followups/phase-02-owner-gates.md` recording, one section per gate, the question, what it blocks, the exact closing action, and the exact command or owner edit. OD-01 (canonical dataset identity + license + Option A/B/C): closing action is the owner recording dataset identity, license evidence, and bundle policy in ADR-0101, after which a real edition is imported with `qai quran import <manifest> --adapter json`; state explicitly that per D-05 (ADR-0101 Option B) this phase ships no real canonical text, that no dataset slug, publisher, release, hash, or license was invented by this phase, and that the synthetic fixtures remain non-canonical. OD-02 (named reviewer + `verified_by`): closing action is the owner naming reviewer, comparison sample, and method, after which the stamp is applied with `qai quran edition verify <slug@version> --reviewer <name> --method <method> --yes`; note that any reviewer string currently present in the tree is a test-only placeholder and must be labelled as such. OD-03 (independent reference corpus): closing action is the owner naming corpus, comparison procedure, and signer in ADR-0114, after which `qai quran import <manifest> --reference <reference-path>` evaluates QV-015 against that named corpus; record the verified fact that the `spqrxi/quranchecksum` candidate is hash-only and cannot by itself satisfy a byte-exact QV-015 comparison, so the owner must decide whether QV-015 v1 remains a checksum comparison or a text-bearing corpus is selected. State in the file that no agent may close any of the three.

    Add a separate **owner-ratifiable interpretation** section to the same file recording that D-15 ("enforce `verify_quotation` on every current answer path") is delivered as: enforce on every path that accepts an *externally supplied* quotation (the stored-verdict HTTP path and the new `qai quran verify-quotation` verb), while the direct-read CLI/HTTP/tool paths (`quran get`/`get_ayah`, `quran.get_context`, and their HTTP/tool mirrors) are **structurally exempt** because they serve bytes from the canonical source of truth and cannot mismatch by construction — wrapping them would add no verification value (research A6/F-6/Pitfall 4). Mark this as an interpretation the owner should ratify or overrule, give each exempt path's file:line basis (per plan 02-05), and state that it is NOT recorded as a closed decision. Also record any coverage shortfall from Task 2 in the same file as a distinct non-owner section. Add a short pointer line to each of the OD-01/OD-02/OD-03 rows in `docs/05-followups/decisions-needed.md` referencing the new record and re-confirming the 🔴 status; do not change any other row and do not flip a gate to resolved.
  </action>
  <verify>
    <automated>grep -c 'OD-01\|OD-02\|OD-03' docs/05-followups/phase-02-owner-gates.md</automated>
    <fails_when>non-zero exit, or a count below 3, or no occurrence of the closing command `qai quran edition verify` in the file</fails_when>
    <automated>grep -n 'ratif' docs/05-followups/phase-02-owner-gates.md && grep -n 'D-15' docs/05-followups/phase-02-owner-gates.md</automated>
    <fails_when>non-zero exit, which means the D-15 read-path exemption was not recorded as an owner-ratifiable item</fails_when>
    <automated>grep -n 'phase-02-owner-gates' docs/05-followups/decisions-needed.md</automated>
    <fails_when>non-zero exit, which means the decisions ledger does not point at the new gate record</fails_when>
  </verify>
  <done>All three owner gates are recorded as blocked with a named closing action and command and marked agent-uncloseable; the D-15 read-path exemption is recorded as an owner-ratifiable interpretation (not a closed decision); the decisions ledger points at the record; no gate is marked resolved.</done>
  <acceptance_criteria>
    - The record names each of OD-01, OD-02, OD-03 with its question, what it blocks, the closing action, and the exact command/owner edit.
    - The record states that no dataset, license, reviewer, reference-corpus identity, or signer was invented, and that the synthetic fixtures stay non-canonical.
    - The record notes the hash-only limitation of the `spqrxi/quranchecksum` candidate for a byte-exact QV-015 comparison.
    - A distinct owner-ratifiable section records the D-15 read-path exemption with the per-path basis and states it is not a closed decision.
    - `docs/05-followups/decisions-needed.md` still shows all three gates 🔴 and now references the record.
    - No ✅/resolved marker appears for any of the three gates.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reconcile the coverage gate for the Quran crates</name>
  <files>xtask/src/coverage.rs</files>
  <read_first>
    - xtask/src/coverage.rs (`THRESHOLDS`, `coverage_by_crate`, `violations`, `run()`)
    - xtask/src/ci.rs (how the coverage step is invoked in the pre-merge gate)
    - docs/03-plan/phases/phase-01-core/acceptance.md §4 (the legacy Quran coverage floors and the OD-07 ratification claim)
    - .planning/phases/02-canonical-quran-core/02-RESEARCH.md § QC-12
    - .planning/codebase/TESTING.md (coverage gate invocation)
  </read_first>
  <action>
    Reconcile the published floors with the enforced gate. Measure the actual per-crate coverage for `crates/quran-core/`, `crates/quran-corpus/`, and `crates/citations/` by running `cargo llvm-cov --workspace --lcov --output-path lcov.info` and then `cargo run -q -p xtask -- coverage-gate lcov.info`. Add a `Threshold` row for each of the three crates to `THRESHOLDS` in `xtask/src/coverage.rs`. Where the measured value is at or above the legacy floor recorded in `docs/03-plan/phases/phase-01-core/acceptance.md` §4, use the legacy floor; where it is below, set the threshold to the measured floor and add a follow-up entry (in `docs/05-followups/phase-02-owner-gates.md`, as a separate non-owner "coverage shortfall" section) naming the legacy floor, the measured value, and the gap to close — never set a threshold above measured to force red CI, and never leave a published floor unimplemented without a recorded shortfall. Keep the existing rows unchanged and keep the unit tests in the same file green. Do not enable `pedantic`/`nursery` lints or change unrelated gate steps.
  </action>
  <verify>
    <automated>cargo llvm-cov --workspace --lcov --output-path lcov.info && cargo run -q -p xtask -- coverage-gate lcov.info</automated>
    <fails_when>non-zero exit, or output reporting a per-crate violation for quran-core, quran-corpus, or citations</fails_when>
    <automated>cargo test -p xtask -- --nocapture</automated>
    <fails_when>non-zero exit, or "0 passed", or a threshold unit test failing after the new rows were added</fails_when>
  </verify>
  <done>The three Quran crates have explicit coverage thresholds enforced by `coverage-gate`, and any measured shortfall against the legacy published floors is recorded with its gap rather than left silent.</done>
  <acceptance_criteria>
    - `THRESHOLDS` contains rows for `quran-core`, `quran-corpus`, and `citations`.
    - `cargo run -q -p xtask -- coverage-gate lcov.info` exits zero on the measured report.
    - Every threshold at or above the legacy floor is stated in its rationale; every threshold below it has a recorded shortfall entry with the measured value and the gap.
    - No existing threshold row changed, and no unrelated CI step changed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Commit the evidence of record, the integrity artifact, the edge-probe disposition, and the closed TASK-002 record</name>
  <files>docs/06-progress/corpus-integrity-report.json, docs/06-progress/phase-02-evidence.md, docs/06-progress/task-done-rollup.md, docs/04-tasks/active/TASK-002-canonical-quran-core.md, docs/04-tasks/active/README.md, docs/04-tasks/completed/TASK-002-canonical-quran-core.md, docs/04-tasks/completed/README.md, crates/cli/tests/corpus_integrity.rs</files>
  <read_first>
    - crates/application/src/quran_cli.rs (`cmd_quran_verify` JSON shape from plan 02-03; avoid volatile fields)
    - crates/cli/tests/quran/read_flow.trycmd (the migrate → import → activate → read ritual used to produce reproducible output)
    - fixtures/quran/test-edition-rich/manifest.json (the fixture the artifact is generated from)
    - .planning/ROADMAP.md § Phase 2 (the five success criteria to evidence)
    - .planning/phases/02-canonical-quran-core/02-VALIDATION.md (the quick/integration/surface/full command set)
    - docs/06-progress/task-done-rollup.md (the progress/evidence tree convention from AGENTS.md)
    - docs/04-tasks/active/README.md and docs/04-tasks/completed/README.md (the active→completed index contract)
    - .planning/phases/01-foundations/01-05-PLAN.md (the Phase 1 task-record lifecycle this mirrors)
    - AGENTS.md (Completion Rules: task doc → rollup → phase progress)
  </read_first>
  <action>
    Produce and commit the evidence of record. Generate `docs/06-progress/corpus-integrity-report.json` by running the verified operator surface against a fresh temporary data directory: migrate, import the rich synthetic fixture with its companion synthetic reference, activate, then `qai quran verify --json`. The artifact must contain the six family keys, the pinned edition identity (`slug`, `version`), each family's status, and the reference family's evidence object; it must contain no timestamp, no temporary path, no host name, and no random id, so that it is reproducible. If the JSON currently carries volatile fields, prune them in the committed artifact and state the exact regeneration ritual in the evidence document — do not add a hidden filter to the command. Add `crates/cli/tests/corpus_integrity.rs` (a new harness, independent of the existing `tests/quran.rs`) that parses the committed artifact, asserts exactly the six family keys are present, asserts the reference family status is one of `pass`/`fail`/`skipped` and that a `skipped` value is never serialized as `pass`, and asserts the pinned edition identity matches the rich fixture's manifest.

    Then write `docs/06-progress/phase-02-evidence.md` mapping each of the five roadmap success criteria to: the exact command, the observed result, and the artifact or test that proves it — including criterion 1 (rollback rejection evidence from 02-04), criterion 2 (byte-exact lookup plus its stored hash on the operator surface), criterion 3 (the committed integrity report and the six-family `verify` surface from 02-03), criterion 4 (per-table trigger aborts from 02-04 and the importer code-path audit), and criterion 5 (the `verify-quotation` mismatch exit and HTTP enforcement from 02-05). State at the top of the document, per D-02, that the phase contract being evidenced is `.planning/ROADMAP.md` §Phase 2 plus `.planning/REQUIREMENTS.md` (`REQ-quran-corpus`, `REQ-data-separation-layers`, `REQ-ingestion-validation-eval`), and that the legacy board (`docs/03-plan/phases/phase-01-core/`), the `specs/005·006·012·015·016·019` documents, and ADRs 0101–0114 are authoritative evidence/reference only — their own recorded status (for example the legacy ledger's 0-of-21 verification) must not be restated as this phase's completion, and the legacy numbering drift (`docs/03-plan/phases/phase-02-rag/` is search/linguistics, i.e. this roadmap's Phase 3) must be stated rather than resolved by renumbering directories.

    In the same document, add a **spec-less edge-probe disposition** section that enumerates ALL six unresolved rows from the fallback run over this phase's requirements — REQ-quran-corpus: `empty`, `encoding`; REQ-data-separation-layers: `unclassified`; REQ-ingestion-validation-eval: `empty`, `encoding`, `concurrency` — and for each row names where it is answered: `empty` → the absent-identity/absent-license criteria in plan 02-01 and the recorded QV-015 skip criterion in plan 02-03; `encoding` → the byte-exact identity/NFC criteria in plans 02-01 and 02-02 and the `sha256:<hex>` reference-hash criterion in plan 02-03; `concurrency` → the migration-interruption criterion in plan 02-01 and the concurrent pointer-change criterion in plan 02-04; `unclassified` (REQ-data-separation-layers) → the layer-separation negative test in plan 02-06. No row may be omitted or left without a disposition. Also record the read-path exemption list from plan 02-05 (which answer paths are enforced versus structurally exempt, with a basis for each) and state that the D-15 exemption is owner-ratifiable per `docs/05-followups/phase-02-owner-gates.md`, and record the known legacy baseline that `quran_search_snapshots`/`quran_normalize_snapshots` belong to the search/normalization phase and must be re-checked on a quiet worktree before any phase-gate read.

    Finally, close the repo task record per AGENTS.md's completion flow. Create `docs/04-tasks/active/TASK-002-canonical-quran-core.md` recording the phase task: identity, the phase contract reference (`.planning/ROADMAP.md` §Phase 2 + the three requirement ids), the plan links 02-01…02-07, and acceptance criteria mapped to the five roadmap success criteria. Link it from `docs/04-tasks/active/README.md`. After the gate evidence and this evidence document exist, add a dated completion section with the recorded commands/exit codes, remove the TASK-002 line from the active index, add exactly one dated `TASK-002-canonical-quran-core` link to `docs/04-tasks/completed/README.md`, move the file to `docs/04-tasks/completed/TASK-002-canonical-quran-core.md` without changing its id, and add a newest-first entry to `docs/06-progress/task-done-rollup.md`. Do not mark the task complete before the evidence exists, and do not rewrite or reorder unrelated index or rollup entries. Run the full gate set and record its actual output.
  </action>
  <verify>
    <automated>cargo test -p cli --test corpus_integrity -- --nocapture</automated>
    <fails_when>non-zero exit, or "0 passed", or the artifact missing a family key or recording a skipped reference family as pass</fails_when>
    <automated>python3 -c "import json,sys; d=json.load(open('docs/06-progress/corpus-integrity-report.json')); ks=set(d.get('families',d).keys()); need={'counts','addressing','unicode','checksums','roundtrip','reference_comparison'}; sys.exit(0 if need<=ks else 1)"</automated>
    <fails_when>non-zero exit, meaning the committed artifact does not expose all six named families</fails_when>
    <automated>python3 -c "t=open('docs/06-progress/phase-02-evidence.md').read(); import sys; ok = ('edge-probe' in t.lower()) and ('concurrency' in t) and ('unclassified' in t) and ('encoding' in t) and ('empty' in t) and ('REQ-quran-corpus' in t) and ('REQ-data-separation-layers' in t) and ('REQ-ingestion-validation-eval' in t) and ('ratifi' in t.lower()); sys.exit(0 if ok else 1)"</automated>
    <fails_when>non-zero exit, meaning the edge-probe disposition, read-path exemption, or criteria record is incomplete</fails_when>
    <automated>test -s docs/04-tasks/completed/TASK-002-canonical-quran-core.md && test ! -e docs/04-tasks/active/TASK-002-canonical-quran-core.md && grep -q 'TASK-002' docs/06-progress/task-done-rollup.md</automated>
    <fails_when>non-zero exit, meaning the task record did not complete the active→completed lifecycle or the rollup entry is missing</fails_when>
    <automated>cargo test -p application --test quran_import --test quran_reader --test quran_translation --test quran_verification --test quran_doctor --test quran_tools && cargo test -p storage-sqlite --test quran && cargo test -p cli --test quran --test doctor_json && cargo test -p server --test api</automated>
    <fails_when>non-zero exit, or "0 passed" in any suite summary, or any failure other than the two documented legacy snapshot files</fails_when>
  </verify>
  <done>A committed, reproducible corpus-integrity artifact, a criterion→command→observed-result evidence document that also dispositions all six spec-less edge-probe rows and the read-path exemption, and a closed `TASK-002-canonical-quran-core` record with its rollup entry exist; the full Quran-area suite runs green with only the documented legacy baseline red.</done>
  <acceptance_criteria>
    - `docs/06-progress/corpus-integrity-report.json` is committed, parses, exposes the six family keys plus the pinned edition identity, and contains no timestamp/path/host/random id.
    - The reference family is recorded as `skipped` unless a reference corpus was supplied, and a skipped value is never written as `pass`.
    - `docs/06-progress/phase-02-evidence.md` records each of the five criteria with its command, observed result, and proving artifact/test, plus the read-path exemption list (flagged owner-ratifiable) and the legacy baseline note with the quiet-worktree instruction.
    - The evidence document's edge-probe disposition section lists all six unresolved rows with the requirement id, category, and answering plan/criterion for each — no silent drop.
    - `crates/cli/tests/corpus_integrity.rs` asserts the artifact's shape and honest classification.
    - `TASK-002-canonical-quran-core` exists under `docs/04-tasks/completed/` (not active), maps plans 02-01…02-07 and the five success criteria, and has a newest-first rollup entry.
    - Any red result is attributed to its real owner with the actual observed output recorded, not absorbed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
**Posture:** ASVS Level 1; any unmitigated `high`-severity threat blocks this plan — no auto-approval.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored artifacts → project evidence | Records must not overstate completion; the evidence document is the phase's claim of record. |
| gate configuration → CI trust | Thresholds and checks determine whether published quality claims are enforced. |
| generated artifact → committed evidence | The committed integrity report must be reproducible and must not carry volatile or fabricated values. |
| probe rows → plan coverage | An undispositioned edge-probe row is an invisible coverage gap, the exact failure the probe exists to catch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-27 | Repudiation | owner gates marked satisfied without a human decision | high | mitigate | Record OD-01/OD-02/OD-03 as blocked with a named closing action and command; state explicitly that no agent may close them; the verification greps for a resolved marker absence and for the closing command text. |
| T-02-28 | Repudiation | integrity report overstating a skipped family | high | mitigate | The artifact and its test require the reference family to be `skipped`/`fail`/`pass` from state and forbid `skipped` being serialized as `pass`. |
| T-02-29 | Tampering | coverage threshold lowered silently to force green CI | medium | mitigate | Any threshold below a published legacy floor requires a recorded shortfall entry naming the measured value and gap; existing rows must not change. |
| T-02-30 | Repudiation | misattributing a red suite to another phase | medium | mitigate | Require the actual observed output in the evidence document, plus the quiet-worktree instruction before any phase-gate read. |
| T-02-31 | Tampering | committed artifact containing volatile or fabricated identity | medium | mitigate | Forbid timestamps, temp paths, host names, and random ids in the artifact; require it to be regenerable by the documented ritual from the rich fixture. |
| T-02-37 | Repudiation | spec-less edge-probe row silently dropped | high | mitigate | Enumerate all six rows and their answering plan/criterion in the evidence document; a gate asserts every category token and requirement id is present. |
| T-02-38 | Repudiation | D-15 read-path exemption closed without owner knowledge | high | mitigate | Record it as an explicit owner-ratifiable interpretation with per-path basis; never present it as a satisfied locked decision. |
| T-02-39 | Repudiation | task marked complete before its acceptance criteria hold | medium | mitigate | The TASK-002 record moves active→completed only after the gate evidence and evidence document exist; the rollup entry is required. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install occurs in this phase (RESEARCH.md § Package Legitimacy Audit). Any future install requires the package-legitimacy gate plus a blocking human checkpoint. |
</threat_model>

<verification>
- `cargo run -q -p xtask -- coverage-gate lcov.info` — thresholds for the three Quran crates enforced.
- `cargo test -p cli --test corpus_integrity` — committed artifact shape and honest reference-family classification.
- `python3` check on `docs/06-progress/phase-02-evidence.md` — all six edge-probe rows + the read-path exemption + the criterion record present.
- `test -s docs/04-tasks/completed/TASK-002-canonical-quran-core.md && test ! -e docs/04-tasks/active/TASK-002-canonical-quran-core.md` — the task record completed its lifecycle.
- Full Quran-area suite (import, reader, translation, verification, doctor, tools, storage-sqlite quran, cli quran + doctor_json, server api) — green except the documented legacy snapshot baseline.
- `cargo run -p xtask -- ci` — full pre-merge gate on a quiet worktree, output recorded.
- Manual/owner: OD-01, OD-02, OD-03 remain blocked and are the only items preventing real canonical activation; the D-15 read-path exemption awaits owner ratification.
</verification>

<success_criteria>
- All five Phase 2 success criteria have recorded command output and a proving artifact, or a recorded blocked gate naming exactly why not.
- The corpus-integrity report is committed as evidence of record and classifies the reference family honestly.
- Every spec-less edge-probe row and the D-15 read-path exemption are dispositioned in writing; no silent drop or silent closure.
- The coverage gate enforces or explicitly records the published Quran-crate floors.
- `TASK-002-canonical-quran-core` is closed through the AGENTS.md lifecycle with a rollup entry.
- OD-01/OD-02/OD-03 are recorded as human-only gates with their closing commands, and the phase is honest about real activation remaining blocked.
</success_criteria>

## Artifacts this plan produces

| Artifact | Kind | Consumer |
|---|---|---|
| `docs/06-progress/corpus-integrity-report.json` | committed evidence artifact | phase evidence, operator/CI reproduction |
| `docs/06-progress/phase-02-evidence.md` | evidence document (criteria + edge-probe disposition + exemption) | verification, milestone audit |
| `docs/05-followups/phase-02-owner-gates.md` | owner-gate ledger + owner-ratifiable D-15 item | owner decisions, OD closure |
| `docs/05-followups/decisions-needed.md` (pointer lines) | decisions ledger | owner decisions |
| `docs/04-tasks/completed/TASK-002-canonical-quran-core.md` (+ active/completed README updates, rollup entry) | closed task record | AGENTS.md completion flow, audit |
| `crates/cli/tests/corpus_integrity.rs` | test harness | `cargo test -p cli` |
| `xtask/src/coverage.rs` THRESHOLDS rows for quran-core / quran-corpus / citations | gate config | `xtask coverage-gate`, CI |

<output>
Create `.planning/phases/02-canonical-quran-core/02-07-SUMMARY.md` when done
</output>
