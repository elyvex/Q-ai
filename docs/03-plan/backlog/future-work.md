# Future Work

Seeded 2026-09-24 as a pointer file (was 0 bytes). Longer-horizon items parked
out of the active phases — see `backlog.md` for the triaged list and the
phase `plan.md` files for out-of-scope sections. Promote via `backlog.md`,
never by direct implementation.
