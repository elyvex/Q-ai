# Phase 3 (server) — Task Board — placeholder

**Status:** No accepted server phase. This directory is a legacy planning
placeholder (see `README.md`); its board files carry this notice instead of
tasks so that empty files are not mistaken for missing plans.

Server code that exists today (`crates/server`, launched by `qai serve`):
loopback-only Quran read API, normalization preview/profile routes, health
endpoints, labelled debug reader. It was built under Phase-0 (serve stub),
Phase-1 (API v1), and Phase-2 (P2-T51 search endpoints) task IDs — see those
phases' `tasks.md`/`done.md`, not this directory.

Production multi-user deployment, auth/RBAC, and TLS remain future work
(Phase 11 scope). If a server phase is ever planned, its TASK-IDs go here.
